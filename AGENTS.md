# AGENTS.md

bobo：本地技能与 OpenCode 智能体管理界面。原生 HTML/CSS/JS + Node.js 内置 `http` 服务，Node.js 22+、纯 ESM。无 npm 依赖、无构建步骤、无数据库。功能与行为细节见 `README.md`。

## 命令

- 启动：`npm start`（即 `node server.mjs`，加 `--open` 会打开浏览器）；固定监听 `127.0.0.1:4318`，`PORT` 可改。
- 全量测试：`npm test`（依次运行 `test.mjs`、`ai.test.mjs`、`reader.test.mjs`、`streaming.test.mjs`、`sources.test.mjs`、`agents.test.mjs`、`codex-agents.test.mjs`、`opencode.test.mjs`、`codex.test.mjs`、`otty.test.mjs`、`usage.test.mjs`）。
- 单个测试文件：`node --test test.mjs`。
- 没有 lint / typecheck / formatter 配置，`npm test` 是唯一验证手段。
- 测试专用环境变量：`BOBO_HOME`（隔离 HOME）、`BOBO_NPX`（模拟 CLI）、`PORT`。测试全部使用临时目录与本机 mock 服务，不使用真实 HOME、真实技能或真实 API Key；不要拿真实环境当测试环境。

## 架构

- `server.mjs`：唯一 HTTP 入口，包含全部 `/api/*` 路由、CLI 子进程调用、备份、静态资源服务。每个进程生成随机 token；API 需带 `x-bobo-token` 头并通过 Host/Origin 校验（仅允许 `127.0.0.1`）。`index.html` 中的 `__TOKEN__` 在响应时替换。启动时会把旧数据目录 `~/.skills-manager` 逐项迁移到 `~/.bobo`。技能列表在内存里有一份快照（`catalog`，30 秒 TTL）：`list()` 过期后会同步跑一次 CLI，只有列表与刷新类接口该等它；`/api/tree`、`/api/file`、`/api/ai/document` 都经 `skill(id)` 只读这份快照，快照里没有才刷新——读取路径等 CLI 会让文件树和阅读区空白。
- `ai.mjs`：OpenAI 兼容客户端。Key 解析顺序为进程环境变量 → macOS `launchctl` → `~/.zshrc` → 手动配置（`~/.bobo/ai.json`，权限 0600）。使用内置 `fetch`，不要引入 SDK 或第三方依赖。
- `reader-ai.mjs`：技能与智能体共用一套快照/缓存/翻译管线（`createSkillReader` / `createAgentReader` / `createCodexAgentReader`）：技能按目录快照，智能体按单个文件快照（OpenCode 是 Markdown，Codex 是 TOML，都是单文件）。缓存目录对技能保持 `digest(id)`（兼容既有缓存），智能体用 `digest('agent:'+id)` 区分，Codex 智能体用 `digest('codex-agent:'+id)`，同名 id 互不覆盖。翻译分批（首批 2 段，之后每批 6 段，2 个 worker 并行），`ai.generate` 的 `subject` 决定提示词里称「技能」还是「智能体」；技能文案必须保持逐字不变（缓存与测试依赖）。
- `sources.mjs`：「我的 Skill」的目录配置（`~/.bobo/sources.json`）、技能发现、`~/.agents/skills` 符号链接同步、系统文件夹选择器（`osascript`，仅 macOS），以及 `git`/`gh` 子进程封装（GitHub 同步）。`git`/`gh`/`osascript` 参数一律用数组 + `shell:false`，远程地址与脚本内容先校验再执行。
- `agents.mjs`：「智能体」视图的数据层，直接管理 OpenCode V2 的全局 agent 定义文件 `~/.config/opencode/agents/**/*.md`。文件名（含子目录）就是 ID，`agentIdOK` 限制每段为小写字母、数字、短横线；保存校验版本 hash，符号链接只读不可写。这里的 agent 没有 CLI 可管理（`npx skills` 只管技能，`opencode` V2 没有 agent 子命令），不要试图接 CLI。
- `codex-agents.mjs`：「智能体」视图 Codex 来源的数据层，管理 `~/.codex/agents/**/*.toml`（Codex CLI 的自定义 subagent 定义）。文件名（含子目录）是 ID，`codexIdOK` 限制每段为字母、数字、短横线、下划线；Codex 实际以 TOML 里的 `name` 字段识别，文件名只是约定（界面会提示两者不一致）。支持读取 / 编辑 / 新建 / 重命名 / 删除，没有「停用」（Codex 无此概念，界面不显示开关）。`public/codex-format.js` 是不依赖 DOM 的 TOML 顶层字段解析、模板与校验，服务端与浏览器共用。
- `backups.mjs`：备份轮换。备份目录按 `<ISO 时间>-<操作>` 命名，`pruneBackups` 按名称排序只保留最近 3 份（LRU）；技能与智能体的备份共用 `~/.bobo/backups` 与同一配额。改保留数量只需改调用处的 limit 或默认值。
- `opencode.mjs`：「通知岛」的数据层。读 `~/.local/state/opencode/service.json`（`XDG_STATE_HOME` 优先）拿服务地址与密码，用 Basic auth（用户 `opencode`）长连 `GET /api/event`，解析 SSE 的 `data:` 行维护各会话状态；断线 2 秒、服务未运行 5 秒后重连，端口变化自动跟上。通知走 `osascript`（提示文本按 AppleScript 字符串转义后拼接，`shell:false`）与 `afplay`。会话列表按**最近一次状态变更**倒序（`changedAt`/`changeSeq`，只有状态真的变了才刷新；工具调用、文本输出等活动事件不重排，列表不会来回跳），任何状态变更都会把会话顶到最前。`acked` 表示用户已在终端看过该会话：点会话行跳 Otty 成功、或 Otty 在前台且正停在那个标签页（`server.mjs` 每 3 秒用 `otty.viewed(opencode.unviewed())` 判断后调 `acknowledge`）才置位；结束/终止后留在折叠态的头像靠它决定何时消失（状态再变会重新变成未查看）。设置存 `~/.bobo/opencode.json`：`notify`、`sound`、`notch`、`hideWhenIdle`、`autoExpand`、`rows`（1–8）、`menubar`（默认 false，是否有菜单栏图标）、`movable`（默认 false，刘海是否可拖）、`display`（`auto`/`builtin`/`main`，面板在哪块屏）。加设置项时三处要同步：这里的默认值与 `loadSettings`/`saveSettings`、Swift `IslandSettings` 的解码、网页的气泡开关。
- `codex.mjs`：「通知岛」里 Codex CLI 会话的数据层。Codex 没有事件流服务，改为每 2 秒轮询 `~/.codex/sessions/<年>/<月>/<日>/rollout-*.jsonl`（只扫最近两天、按 mtime + size 判断变化，只读文件头尾）；从 `session_meta` 拿 session_id 与 cwd，从尾部 `event_msg` 推导状态：`task_started`→运行中、`task_complete`→已结束、`turn_aborted`→已终止（若出现审批类事件则→等你回答）。标题读 `~/.codex/session_index.jsonl` 的 `thread_name`。结束 / 终止挂起 1500ms 再提醒（复用 `opencode.remind`，受通知岛开关控制），有新活动就撤销；结束 / 终止的会话保留 30 分钟、按 `acked` 决定头像是否消失。会话 id 统一加 `codex:` 前缀，`source` 字段区分来源；`server.mjs` 把两个 Agent 的会话合并进状态流（各自给 `order`，按最近一次状态变更倒序），并让 Otty 的「看过了」轮询同时覆盖 Codex。
- 「通知岛」的事件名是 V2 的公开事件流：`session.inbox.enqueued` / `session.execution.started|succeeded|failed|interrupted` / `form.created|replied|cancelled` / `session.renamed` / `session.created` / `permission.asked`。提问走的是 **form**（不是 `question.asked`，那是 V1），所以「等你回答」必须认 `form.created`；`session.status`(idle/busy) 在事件流里不可靠，结束/终止一律以 `session.execution.*` 为准。
- 「通知岛」是第三个一级视图（`index.html` 的 `islandTab` + `islandWorkspace`，`app.js` 的 `switchView`/`renderIsland`）。`switchView` 用 `islandView` 记当前视图，进入时订阅 `/api/opencode/stream`、离开时断开（`openIsland`/`islandClose`）；`agentActive` 只是「是否在智能体视图」的派生值，⌘S 保存逻辑依赖它，别删。顶栏的技能专属操作（添加技能、同步/更新全部）由 `body[data-view]` + CSS 收敛（`body:not([data-view=skills]) header>nav{display:none}`），只在技能视图出现；新增一级视图时不用再写一处判断。通知岛本身也是「左侧分类 + 右侧分区」：侧栏用 `settings-menu` 分两组——**通知岛设置**（提醒 / 刘海面板 / 菜单栏与位置，`data-island-pane`）与 **Agent 连接**（`opencode` 与 `codex` 两个分栏，条目右侧的 `#islandNavDot`/`#islandCodexDot` 跟着各自最新会话状态变色，`renderIsland` 里更新）；`showIslandPane` 只切分栏显隐，`renderIslandSettings` 在分栏里渲染开关（行 id 还是 `rowNotify`/`rowNotch`/`rowMenubar`/…），**会话列表按来源分开放在两个分栏里**（OpenCode 用 `#islandList`/`#islandCount`，Codex 用 `#codexList`/`#codexCount`，都由 `renderIsland` 从合并后的 `sessions` 里按 `source` 过滤）。左侧底栏的「设置」按钮是所有视图共用的 `.open-settings`。
- 「用量」是第四个一级视图（`index.html` 的 `usageTab` + `usageWorkspace`，`app.js` 的 `openUsage`/`usageClose`，进入时 `loadUsage` 并用 60 秒定时器刷新）。侧栏是 `settings-menu` 的来源分栏（`data-usage-pane` 的 Codex / OpenCode Go，条目右侧 `#usageDotCodex`/`#usageDotOpencode` 表示是否可用，点分栏会同时 `POST /api/usage/provider` 把刘海胶囊切到这家）；「刷新额度」是右下角的悬浮按钮（`#usageRefresh`，与命令行凸起同位置，只在用量视图显示，不是侧栏菜单项）。右栏每个来源顶部有一条启停开关（`#rowUsageCodex`/`#rowUsageOpencode` → `POST /api/usage/enabled`，关掉的来源不参与刘海胶囊的点击循环，数据照常读取）；OpenCode Go 分栏里还有 API Key 表单（`#usageKeyForm`，保存前先调接口验证、清除后回退到环境变量/本机登录，页面不回显 Key）、`#usageDays`（最近 30 天）与 `#usageModels`。窗口卡片用 `usage-grid`/`usage-card`/`meter`，`usageRow` 用 `.usage-day` 网格（日期/条形/数值），模型行加 `.model`。
- 「设置」不是常驻 tab：侧栏左下角底栏的 `.open-settings` 按钮（三个视图各一个，底栏用 `.library>footer{margin-top:auto}` 钉在左下角）调用 `openSettings`，把 `#settingsTab` + `#settingsClose` 显示出来并切到 `settingsWorkspace`；`switchView('settings')` 记下来源视图 `settingsBack`，点 × 的 `closeSettings` 回到它。`settingsWorkspace` 是「左侧分类导航 + 右侧分区卡片」：左边 `settings-nav`（`data-settings-pane` 的外观 / AI / 关于，`showSettingsPane` 只切面板显隐），右边 `settings-pane` 里是 `settings-card`/`settings-row`（标签左、控件右；窄控件加 `class="narrow"`）。**刘海与菜单栏设置不在这里**：`notch`/`autoExpand`/`hideWhenIdle`/`rows`/`menubar`/`movable`/`display` 都在「通知岛」视图（见上一条）。进入设置时 `switchView` 只调 `loadAISettings`。AI 表单的 id 没变（`aiSettingsForm`），"AI 设置"按钮与 `aiSettingsDialog` 已删除。`data-view=settings` 时顶栏操作同样收起，`#console`/`#consoleHandle` 也只在技能 / 智能体视图显示。
- `otty.mjs`：点击会话跳到 Otty 标签页。先 `otty-cli tab list --json`，按标题精确匹配 `OC | <会话标题>`（Otty 的 opencode 集成用会话标题命名标签页），其次按目录（目录下只有一个标签页时），命中后 `otty-cli tab focus <id>` 并 `open /Applications/Otty.app` 激活；找不到就只打开 Otty。匹配规则抽成导出的纯函数 `matchTab(tabs,{title,directory,source})`（默认 OpenCode 匹配 `OC | <标题>`；Codex 依次尝试 `CX | `、`Codex | `、`CDX | `，再退回标题包含与目录），`focus` 与「看过终端」共用。`viewed(sessions)` 用纯函数 `viewedIds` 判断用户是否已经看过某个会话的终端：只有当 **Otty 是前台应用**（`lsappinfo front` 的 bundleID 为 `io.appmakes.otty`）且该会话的标签页是**当前聚焦窗口**里 `active` 的那一个时才算——Otty 在后台时「窗口内选中」不算看过，避免头像还没被看到就消失。`otty-cli` 固定读 `/Applications/Otty.app/Contents/MacOS/otty-cli`，参数用数组 + `shell:false`；`server.mjs` 每 3 秒把两个 Agent 的未查看会话合并后轮询一次（没有未查看的会话时直接返回，不落子进程）。网页和通知岛面板都走 `POST /api/opencode/focus`（`Bobo.swift` 的 `focusIsland` 用 `apiToken` 调同一个接口），跳转成功也会顺带标记已查看。
- `usage.mjs`：用量（Codex + OpenCode Go 两家）。Codex 读 `~/.codex/auth.json`（`CODEX_HOME` 优先）的 OAuth token 调 `https://chatgpt.com/backend-api/wham/usage`，窗口按 `limit_window_seconds` 归类（18000 = 5 小时、604800 = 一周，与 CodexBar 的归一化一致），**只读凭据：不刷新 token、不写回**；登录过期提示在终端跑一次 `codex`，只有 API Key 时说明额度需要 ChatGPT 订阅。OpenCode Go **API 优先**：key 取 `OPENCODE_API_KEY`（环境变量，CodexBar 的顺序）或 `~/.local/share/opencode/auth.json` 里 opencode-go 的 key，调 `https://opencode.ai/zen/go/v1/usage`，`usage.{rolling,weekly,monthly}.percent` 是「已用百分比」、`resetsAt` 是 ISO 时间、`status=rate-limited` 表示已限额（与 CodexBar 一致）；接口失败或没有 key 时退回本机估算：只读 `~/.local/share/opencode/opencode.db`（没有 `-wal/-shm` 时 `immutable=1` 直读，绝不新建 sidecar），取 `opencode-go` 的步骤费用（`step-finish` 分片优先、消息级兜底），按 12 / 30 / 60 美元折算 5 小时 / UTC 周 / 账单月（账单月用最早一条本机记录锚定），并在 `error` 里说明降级原因。快照：`{available,selected,providers:[{id,name,symbol,available,enabled,keySource,keyHint,reason,error,plan,credits,source,estimated,windows:[{key,label,status,usedUSD,limitUSD,usedPercent,remainingPercent,resetInSec,resetsAt}],daily,models,totals}]}`；`source:'api'` 走网络（3 分钟节奏、硬下限 60 秒），`source:'local'` 每 60 秒，`resetInSec`/`resetsAt`/`updatedAt` 不参与去重（数据没变不推送）。设置存 `~/.bobo/usage.json`（权限 0600）：`provider`（刘海胶囊显示哪家）、`enabled`（来源启停：只有「可用且开启」的才参与 `POST /api/usage/provider` 的选择与循环，关掉当前那家会自动换一家）、`keys`（手动填的 Key，优先级高于环境变量与本机登录）。`POST /api/usage/enabled {id,enabled}` 启停来源；`POST /api/usage/key {key}` 保存前先调一次接口验证（失败不落盘、返回 `{ok:false,message}`），`{clear:true}` 清除；换 Key / 清除后都先 `cache.delete(id)` 再重读，避免吃旧缓存。`GET /api/usage?refresh=1` 强制刷新，但仍受硬下限保护。用量挂在 `/api/opencode/stream` 快照的 `usage` 字段里，原生不用另外轮询。
- 网页与原生之间的通道：两个 `WKWebView` 都注册了 `window.webkit.messageHandlers.bobo`（`Bobo.swift` 的 `userContentController`），命令有 `open`、`focus`、`size`、`visible`、`status`；`bobo://` 导航是同样命令的回退路径（在浏览器里打开 `notch.html` 时用）。`size`（窗口尺寸）和 `visible`（是否显示）都由网页决定，原生只执行——这样透明窗口的点击区域永远等于内容大小。
- `public/markdown.js`、`public/stream.js`、`public/agent-format.js`、`public/codex-format.js` 同时被服务端代码和浏览器代码 import（`reader-ai.mjs` / `ai.mjs` / `agents.mjs` / `codex-agents.mjs` 引用 `public/` 下模块）。`splitMarkdown` 的分段 ID 同时决定翻译对齐与阅读渲染，修改后两端都要验证。`renderBlock` 依赖 `document`，只能在浏览器使用；`frontmatterRows` 是不依赖 DOM 的纯函数（声明块表格的解析），可直接单元测试。`agent-format.js` 的 frontmatter 解析与模板同时决定列表展示、校验与新建内容。
- `server.mjs` 的静态资源是硬编码白名单（`/`、`/app.js`、`/style.css`、`/markdown.js`、`/stream.js`、`/agent-format.js`、`/codex-format.js`）；新增 `public/` 文件必须同时注册，否则 404。CSP 禁止内联脚本（所以样式都放在 `style.css` 里，页面不能带 `<style>`）。`/` 会替换 `__TOKEN__`。
- 顶栏 tab 与侧栏「设置」按钮的图标是 SF Symbols：`index.html` 顶部内联了一个 `.sf-sprite`（`sf-stack` 技能 / `sf-person2` 智能体 / `sf-bell` 通知岛 / `sf-gauge` 用量 / `sf-gearshape` 设置，取自 Apple SF Symbols 的 SVG 导出，路径里已把 black 换成 `currentColor`；设置页分类导航另加三个手写的 16×16 小图标 `sf-circle-lefthalf` 外观 / `sf-sparkles` AI / `sf-info` 关于，通知岛侧栏也用同样手写的小图标 `sf-notch` 刘海面板 / `sf-menubar` 菜单栏与位置 / `sf-terminal` OpenCode，用量视图用 `sf-sparkles`（Codex）/`sf-terminal`（OpenCode Go）/`sf-chart`（刷新额度）），使用时写 `<svg class="sf"><use href="#sf-xxx"/></svg>`。必须内联——`<img>`/外链 SVG 拿不到 `currentColor`，图标就没法跟随深浅色和 tab 选中态。改图标尺寸只动 `style.css` 的 `.sf`。
- `macos/`：AppKit + WebKit 包装应用。`update` 只复制 `package.json`、`server.mjs`、`ai.mjs`、`reader-ai.mjs`、`sources.mjs`、`agents.mjs`、`codex-agents.mjs`、`backups.mjs`、`opencode.mjs`、`codex.mjs`、`usage.mjs`、`otty.mjs` 和 `public/`，先构建到临时目录，再退出正在运行的旧应用、覆盖安装到 `/Applications/bobo.app` 并打开（可传路径参数）；默认安装位置下会顺带退出并移除旧包 `/Applications/Skills Manager.app`。新增服务端模块必须同步更新 `update`，否则应用包缺文件。Swift 端硬编码 4318 端口。
- 刘海面板（`Bobo.swift` 的 `NotchPanel`/`IslandView`/`IslandModel`）：窗口是无边框 `NSPanel`（`constrainFrameRect` 直接放行，否则系统会在屏幕重排时把它压到菜单栏下方），层级 `mainMenuWindow + 2`、全空间可见、不抢焦点；**内容用 SwiftUI 画**（`IslandShape` 顶部与屏幕边齐平、底部 0.62 连续曲率圆角，折叠 12 / 展开 22，参考 CodeIsland 的 NotchPanelShape）。窗口顶部始终贴住所选屏幕顶边（`y = frame.maxY - height`），尺寸由原生按内容算：折叠是「刘海宽度 + 80」的胶囊，展开再加出卡片（每行 31pt）。
- 面板显示在哪块屏由 `chosenScreen()` 统一决定（设置 `display`）：`auto`（默认）用 `frontmostWindowBounds()`（`CGWindowListCopyWindowInfo` 的 layer 0 窗口，排除 bobo 自己）取前台应用窗口所在屏，拿不到就保持 `currentScreenSignature` 记的当前屏，再退到有刘海的屏、主屏；`builtin` 固定有刘海的屏，`main` 固定主屏。**主屏是 `NSScreen.screens[0]`（带菜单栏的那块），不能用 `NSScreen.main`**——后者是「当前键盘焦点窗口所在的屏幕」，会随焦点在显示器之间移动，表现为「选了主屏却跟着焦点跳」（外接屏当主屏时特别明显）。所有取屏处（`positionNotch`/`resizeNotch`/`updateIslandLayout`/拖动 clamp/拖动结束记偏移）都必须走 `chosenScreen()`，别再单独写「第一块刘海屏」。屏幕重判：`didChangeScreenParametersNotification` 走 `handleScreenParametersChange`（立刻 + 0.5 秒各一次，macOS 发通知时 `NSScreen.screens` 可能还没更新），`didActivateApplicationNotification` / `activeSpaceDidChangeNotification` 走 `refreshScreen()`（只在签名变化时重排）。`resizeNotch` 的「没变化就跳过」必须同时比较 `origin.y`，否则切到同宽不同高的屏时不会重贴顶边。拖动时（`islandDragging`）不换屏。
- 通知岛的数据由**原生直接订阅** `GET /api/opencode/stream`（NDJSON，`URLSession.bytes(for:).lines`），token 在主页面 `didFinish` 时用 `evaluateJavaScript` 从 `meta[name=token]` 读一次，断线 2 秒重连。`session.execution.*`、`form.created` 等事件到 `IslandModel` 后同时驱动面板与状态栏图标（`updateNotchStatus`：图标右下角状态点 + 菜单第一行）。**状态栏图标默认不创建**（只留刘海）：`applyIsland` 按快照里的 `settings.menubar` 调 `syncStatusItem`，显示时才 `statusItem(withLength:)` 并按 `statusImage(state:)` 画图标 + 状态点，关掉时 `removeStatusItem`；菜单在 `setupStatusMenu` 里只建一次（`statusItem` 是可选的，用 `statusItem?.button`）。开关在网页「通知岛 → 菜单栏与位置」，写进 `~/.bobo/opencode.json` 后经这条流回到原生。
- 「结束/终止」要**挂起确认**才能落定（`opencode.mjs` 的 `settle`/`cancelIdle`/`ACTIVE`）：收到 `session.execution.succeeded` 后等 1200ms，期间任何活动事件（step/tool/text/reasoning/inbox/compaction）都会取消判定——一次 prompt 里 OpenCode 可能先给完成信号再继续（压缩上下文、工具续跑），直接置 idle 会误报「运行结束」通知。这套判断是 Otty 集成插件的教训（见 `~/.config/opencode/plugins/otty-integration.js` 的注释）；`session.status`(busy/idle) 与 `session.idle` 在公开事件流里目前不出现，代码里仍保留兼容分支。
- 面板动画要和内容同源：**先改模型、让出一帧再动窗口**（`scheduleIslandLayout` → `updateIslandLayout`），否则窗口已经变大而 SwiftUI 还没画内容，展开过程中会出现黑色空隙。窗口尺寸用 `NSAnimationContext`（0.2s easeOut）动画，内容不做 `transition`，靠窗口逐帧变大让列表「抽出来」。折叠态高度按公式与 `IslandView` 常量严格一致：`13.5 + 行数 × 38`（每行固定 36 + 行距 2），`list` 加 `.fixedSize(vertical:)` 防止被压缩。
- 悬停展开（对齐 CodeIsland 的手感）：鼠标移入立即展开、移出延迟 0.35 秒收起；`autoExpand` 打开时状态变化会让面板自动展开——「等你回答」亮 3 秒、会话进入「已结束 / 已终止」亮 2 秒（`applyIsland` 检测到新的等待或 `justFinished` 时调 `revealIslandTemporarily(seconds:)`，把 `IslandModel.autoRevealed` 置 true 并挂 `islandAutoCollapse` 定时），没有操作就收回胶囊；「等你回答」刚被回答时立刻撤销（`autoRevealed` 与 `hovering` 一起清掉，避免鼠标被圈在自动展开的窗口里）；鼠标移入即解除这个限制（`islandHovering(true)` 清掉 `autoRevealed` 并取消定时），之后由悬停保持展开。悬停用 `IslandHostingView` 的 `NSTrackingArea`（`.activeAlways`）自己上报，不用 SwiftUI 的 `onHover`——非激活面板上后者不可靠。点击会话行发 `POST /api/opencode/focus`（后端 `otty.mjs` 负责跳 Otty），点击胶囊打开主窗口；按住胶囊左右拖动（阈值 5pt）移动面板，偏移以**面板中心**为基准记在 `UserDefaults` 的 `islandOffsetX`，状态栏菜单有「通知岛居中」重置；`hideWhenIdle` 打开且没有活跃会话时用 `orderOut` 真正收起窗口。
- 折叠态显示最多 8 个会话头像（`IslandModel.avatars`，含运行中 / 等你回答，以及已结束与已终止但还没在终端看过的（`acked != true`），排序同列表；`IslandAvatar` 是圆角方块脸 + 下方状态灯，底色由会话 ID 的稳定哈希从 8 色板取值，状态灯用会话状态色：蓝=运行中 / 橙=等你回答 / 绿=已结束 / 红=已终止，运行中 / 等你回答时缓慢呼吸），没有活跃会话时用置灰头像占位；最右侧是 `NotchIconButton`（参考 CodeIsland 同名的圆形图标按钮）做的齿轮，点击与点胶囊一样打开主窗口。折叠宽度按头像数量计算，要记得算上齿轮的 22 + 间距 7。展开态才列出会话。
- 应用窗口无标题栏：页面自绘顶栏，红黄绿灯所在的顶部 28pt 由 `TitlebarDragView`（原生拖拽层）覆盖，页面顶栏需留 `padding-top` 让交互元素避开这 28pt。应用加载 `/?app=1`，`app.js` 据此加 `body.app`：`body.app .brand` 绝对定位到顶部标题栏（和红黄绿灯同一水平线），顶栏 grid 里 `nav` 必须显式 `grid-column:3`、`.view-tabs` 显式 `grid-column:1`，否则中间列塌陷后右侧按钮会被自动排到中间。`public/style.css` 默认用 `prefers-color-scheme` 适配深色，另有两套 `:root[data-theme=light|dark]` 覆盖块供「设置 → 外观」手动固定（`app.js` 读写 `localStorage` 的 `bobo-theme`，并兼容读取旧的 `skills-theme`）；新增颜色请同时改这两处，三种外观都要保持可读。
- 窗口显隐动态切换激活策略：应用以 `.accessory` + `LSUIElement` 启动（无 Dock 图标），`showWindow()` 切到 `.regular` 出现在 Dock 与 ⌘Tab，关窗走 `hideWindow()` 切回 `.accessory`。不要改成固定 `.regular`，否则关窗后会一直占用 Dock。

## 约定与陷阱

- 文案、错误消息、注释均为中文，保持一致。
- `normalize()` 是 CLI 命令白名单：只允许 `list`/`find`/`add`/`remove`/`update`/`init`/`--help`/`--version`，自动补 `-g`/`-y`，拒绝 `-p`/`--project`；子进程使用 `shell:false`。不要绕过或放宽。
- `safeFile()` 拒绝绝对路径、隐藏文件/目录、`..` 和符号链接；文件保存校验版本 hash，冲突返回 409。放宽会破坏测试与安全模型。
- 备份规则：编辑文件、`add`/`update` 前、智能体写操作前备份到 `~/.bobo/backups/<时间>-<操作>/`，只保留最近 3 份（LRU 淘汰最旧，技能与智能体共用配额，服务启动时也清理一次）；`remove` 不备份，并在 CLI 执行后直接删除技能目录。调整保留数量时同步更新 `pruneBackups` 的调用与测试。
- 「我的 Skill」的技能在 `~/.agents/skills` 里是指向用户目录的符号链接：CLI 的 `list -g` 能发现，但 `remove` 找不到，所以删除必须走 `sources.removeSkill`（同时清理链接），不要交给 CLI。`unlinkUnder` 只解引用确属该目录的链接，放宽会误删用户文件。
- 「我的 Skill」的停用名单保存在 `sources.json` 各目录的 `disabled` 数组：`relink`/`syncAll` 跳过停用项，停用只删 `~/.agents/skills` 链接、不删源文件；启用前校验同名冲突，删除技能和新建同名技能时要清理名单。
- 「智能体」把 `~/.config/opencode/agents` 里的文件名（含子目录）当作 ID：重命名就是换 ID，已有会话的 `@旧名` 引用会失效；V2 不读取 frontmatter 的 `name` 字段，只作为信息展示。停用只在 frontmatter 写 `disabled: true`，不移动文件。不要程序化改写 `opencode.json(c)` 的 `agents` 字段（JSONC 注释会丢），也不要管理内置 agent（build/plan/general/explore 没有文件）。
- 「智能体」的 Codex 来源把 `~/.codex/agents` 里的 `.toml` 文件当作 ID，但 **Codex 实际以 TOML 的 `name` 字段识别智能体**：重命名只移动文件名，不改 `name`（界面只有 OpenCode 支持「停用」，Codex 没有这个概念）；列表会提示文件名与 `name` 不一致。写操作（save/create/rename/delete）走 `/api/codex/agents/*` 并调用 `codexReader.invalidate`，AI 缓存以 `codex-agent:` 前缀与 OpenCode 智能体隔开。
- 通知岛把 OpenCode 与 Codex 的会话合并成一份 `sessions`（各自带 `source` 与 `order`），刘海面板与网页按 `source` 区分、点击分别路由到 `opencode.acknowledge` / `codex.acknowledge`。新增会话来源时沿用这套：给出 `order` 与 `source`，并接入 `otty.viewed` 的「已查看」轮询。
- 智能体的能力总结/翻译走 `/api/ai/agent/*` 与 `agentReader`；智能体的写操作（save/rename/delete/toggle）必须调用 `agentReader.invalidate`，否则改完文件仍会命中旧摘要/译文。技能与智能体的 AI 缓存以 id 前缀区分，不要把前缀去掉，否则同名 id 会互相覆盖。
- 代码风格高度紧凑（单行函数、少空格），没有格式化工具；改动跟随周围写法，不要整体重排。
