# AGENTS.md

bobo：本地技能与 Agent 管理界面。原生 HTML/CSS/JS + Node.js 内置 `http` 服务，Node.js 22.13+（agy 会话库与 OpenCode Go 本机估算用内置 `node:sqlite`，22.13 起才不需要额外开关）、纯 ESM。无 npm 依赖、无构建步骤、无数据库；技能直接读写 `~/.agents/skills`，不调用 `npx skills`。README 是简介与快速开始，完整功能说明见 `docs/features.md`。

**目录结构**：服务端模块在 `src/`（入口 `src/server.mjs`），测试在 `test/`；网页与通知岛面板在 `public/`；macOS 应用在 `macos/`（`Bobo.swift` + 素材 + 几何测试）；构建与发布脚本在 `scripts/`；跨平台壳在 `v2/`（独立工程，runtime 由脚本从主项目同步）。下文提到 `server.mjs`、`skills.mjs` 等文件名时都指 `src/` 下的实现，测试文件指 `test/` 下的同名 `.test.mjs`（另有 `test/server.test.mjs`）。

## 命令

- 启动：`npm start`（即 `node src/server.mjs`，加 `--open` 会打开浏览器）；固定监听 `127.0.0.1:4318`，`PORT` 可改。
- 全量测试：`npm test`（即 `node --test test/*.test.mjs`，20 个文件：`server`、`ai`、`reader`、`streaming`、`skills`、`sources`、`agents`、`codex-agents`、`configs`、`opencode`、`codex`、`omp`、`claude`、`dsh`、`agy`、`browser`、`terminals`、`usage`、`devices`、`update`）。
- 单个测试文件：`node --test test/skills.test.mjs`。
- 通知岛的几何与补间另有 Swift 侧测试：`node --test macos/island-layout.test.mjs`（不在 `npm test` 里，需要 `xcrun swift`）——它按起止标记从 `Bobo.swift` 截一段纯声明编译成小程序，验证两翼补间不漂移、让位收拢尺寸落在刘海区域内。
- 构建与发布：`scripts/build-app.sh [输出目录]` 构建 `bobo.app`（版本号取 `package.json`，构建编号取 git 提交数或 `BOBO_BUILD`；自动用钥匙串里的 Developer ID 证书签名 + 硬化运行时 + 安全时间戳，没有则退回 ad-hoc 并警告）；`npm run package` 在构建后提交 Apple 公证、staple、Gatekeeper 校验，再输出 `dist/bobo.app.zip` 与 `.sha256`（`BOBO_SKIP_NOTARY=1` 跳过公证，`BOBO_ALLOW_ADHOC=1` 允许未签名发布，公证凭据用 `NOTARY_PROFILE` 或 `NOTARY_API_KEY*`）；`./update.sh [安装路径]` 构建并覆盖安装 `/Applications/bobo.app`（不公证）；打 `v*` tag 后 Release 工作流在有签名 secrets 时自动签名、公证并发布（**不要发成预发布**：应用内更新读 `/releases/latest`，标了 `--prerelease` 的它看不到；beta 轮次把 `package.json` 写成带后缀的同一个版本如 `1.3.0-beta.1`，`parseVersion`/`compareVersions` 按语义化版本比较，同版本的正式版比 beta 新），没有则只跑测试并提示；CI 工作流跑 `npm test`。
- 没有 lint / typecheck / formatter 配置，`npm test` 是唯一验证手段。
- 测试专用环境变量：`BOBO_HOME`（隔离 HOME）、`PORT`。测试全部使用临时目录与本机 mock 服务（技能安装用本地裸仓库），不使用真实 HOME、真实技能或真实 API Key；不要拿真实环境当测试环境。

## 架构

- `server.mjs`：唯一 HTTP 入口，包含全部 `/api/*` 路由、备份、静态资源服务。每个进程生成随机 token；API 需带 `x-bobo-token` 头并通过 Host/Origin 校验（仅允许 `127.0.0.1`）。`index.html` 中的 `__TOKEN__` 在响应时替换。启动时会把旧数据目录 `~/.skills-manager` 逐项迁移到 `~/.bobo`。技能列表在内存里有一份快照（`catalog`，30 秒 TTL）：`list()` 过期后调用 `skills.scan()` 重新扫描本机目录（毫秒级，没有子进程），只有列表与刷新类接口该等它；`/api/tree`、`/api/file`、`/api/ai/document` 都经 `skill(id)` 只读这份快照，快照里没有才刷新。`/api/command` 只接受界面按钮传进来的参数（`normalize()` 校验），实际动作全部由 `skills.mjs` 在服务进程内完成。
- `skills.mjs` / `skill-agents.mjs`：技能的本机数据层，不依赖 `npx skills`。`skill-agents.mjs` 是从 vercel-labs/skills（`skills@1.6.0`，MIT）的 Agent 注册表抽取的目录表（约 77 家有全局技能目录的 Agent，每项含显示名、全局技能目录、安装检测路径、是否为「通用 Agent」），模板变量按 `HOME`/`XDG_CONFIG_HOME`/`CODEX_HOME`/`CLAUDE_CONFIG_DIR` 等解析，解析不出来的候选直接跳过。`skills.mjs` 负责：读写的落盘约定与 skills 生态一致——技能真身放 `~/.agents/skills/<名称>`，其它 Agent 目录里放指向它的相对符号链接（通用 Agent 直接读 canonical，不建链接），来源写在 `~/.agents/.skill-lock.json`（`XDG_STATE_HOME` 优先，version 3）；收录规则是 SKILL.md 的 frontmatter 里 `name` 与 `description` 都非空，`metadata.internal: true` 的不收录。对外只有 `scan`/`add`/`update`/`remove`/`init`/`link` 等方法，`parseSkill`（frontmatter 解析）、`sanitizeName`、`folderHash` 另有命名导出给 `server.mjs` 与测试用。`add` 支持 `owner/repo`、GitHub 树链接、任意 git 地址与本地目录：git 来源 `git clone --depth 1` 到临时目录后复制技能目录，`skillFolderHash` 用 `git rev-parse HEAD:<目录>` 的 tree hash（与 GitHub API / CLI 一致），本地来源用「相对路径 + 内容」的 sha256（与 CLI 的 `computeSkillFolderHash` 一致）；`update` 按 lock 里的来源分组重新拉取，指纹没变就跳过、变了才替换并重新链接；`remove` 同时清理 canonical、各 Agent 目录里的同名技能与 lock 记录，canonical 里指向 `~/.agents/skills` 之外的链接（「我的技能」）会被拒绝并交给 `sources.removeSkill`。
- `ai.mjs`：OpenAI 兼容客户端。Key 解析顺序为进程环境变量 → macOS `launchctl` → `~/.zshrc` → 手动配置（`~/.bobo/ai.json`，权限 0600）。使用内置 `fetch`，不要引入 SDK 或第三方依赖。
- `reader-ai.mjs`：技能与智能体共用一套快照/缓存/翻译管线（`createSkillReader` / `createAgentReader` / `createCodexAgentReader`）：技能按目录快照，智能体按单个文件快照（OpenCode 是 Markdown，Codex 是 TOML，都是单文件）。缓存目录对技能保持 `digest(id)`（兼容既有缓存），智能体用 `digest('agent:'+id)` 区分，Codex 智能体用 `digest('codex-agent:'+id)`，同名 id 互不覆盖。翻译分批（首批 2 段，之后每批 6 段，2 个 worker 并行），`ai.generate` 的 `subject` 决定提示词里称「技能」还是「智能体」；技能文案必须保持逐字不变（缓存与测试依赖）。
- `sources.mjs`：「我的 Skill」的目录配置（`~/.bobo/sources.json`）、技能发现、`~/.agents/skills` 符号链接同步、系统文件夹选择器（`osascript`，仅 macOS），以及 `git`/`gh` 子进程封装（GitHub 同步）。`git`/`gh`/`osascript` 参数一律用数组 + `shell:false`，远程地址与脚本内容先校验再执行。
- `agents.mjs`：「智能体」视图的数据层，直接管理 OpenCode V2 的全局 agent 定义文件 `~/.config/opencode/agents/**/*.md`。文件名（含子目录）就是 ID，`agentIdOK` 限制每段为小写字母、数字、短横线；保存校验版本 hash，符号链接只读不可写。这里的 agent 没有 CLI 可管理（`npx skills` 只管技能，`opencode` V2 没有 agent 子命令），不要试图接 CLI。
- `codex-agents.mjs`：「智能体」视图 Codex 来源的数据层，管理 `~/.codex/agents/**/*.toml`（Codex CLI 的自定义 subagent 定义）。文件名（含子目录）是 ID，`codexIdOK` 限制每段为字母、数字、短横线、下划线；Codex 实际以 TOML 里的 `name` 字段识别，文件名只是约定（界面会提示两者不一致）。支持读取 / 编辑 / 新建 / 重命名 / 删除，没有「停用」（Codex 无此概念，界面不显示开关）。`public/codex-format.js` 是不依赖 DOM 的 TOML 顶层字段解析、模板与校验，服务端与浏览器共用。
- `configs.mjs`：「智能体」视图的全局配置层。`GLOBAL_CONFIGS` 是**只收录「通知岛」已接入 harness 的白名单**（OpenCode、Codex、Claude Code、omp、dsh、agy；其余 Agent 以注释保留，接入通知岛后再开放），每项声明 `dir`（配置根目录，函数，可读 `CODEX_HOME` / `CLAUDE_CONFIG_DIR` / `PI_CODING_AGENT_DIR` / `DSH_HOME` / `ANTIGRAVITY_CONFIG_DIR` / `XDG_CONFIG_HOME` 等环境变量）、`files`（相对根目录的路径，可含子目录）、`icon`（真实品牌图标，取 `index.html` 的内联 SVG 精灵；没有就不放图标，不做首字母占位）、`agents`（子智能体定义后端：`opencode` / `codex` / `null`）。**只认白名单，绝不扫描目录**——避免把 `auth.json`、`*.credentials.json` 之类的凭据暴露出来。`providers()` 给左栏菜单（`installed` = 配置根目录存在**且至少有一份白名单文件**——只被建过目录、例如 bobo 链接技能时建的 `skills` 子目录，不算安装、状态点灰色），`target(provider,key)` 把 provider + 相对路径映射到固定文件并校验不越界；`list` 返回每项的存在与否、大小、mtime、是否符号链接，`read`/`save` 走与智能体一致的规则（1 MB 上限、符号链接只读、版本冲突 409、保存前备份、备份共用 `~/.bobo/backups` 的「最近 3 份」配额）。缺失的文件也返回（`exists:false`），保存时若版本非空则 409，否则创建。`entity(id)` 把 `provider:file` 解析成路径给 AI 阅读管线用。新增 Agent 只需在这里加一项（前端菜单由 `/api/configs/providers` 驱动，不用改 HTML）。
- `backups.mjs`：备份轮换。备份目录按 `<ISO 时间>-<操作>` 命名，`pruneBackups` 按名称排序只保留最近 3 份（LRU）；技能与智能体的备份共用 `~/.bobo/backups` 与同一配额。改保留数量只需改调用处的 limit 或默认值。
- `opencode.mjs`：「通知岛」的数据层。读 `~/.local/state/opencode/service.json`（`XDG_STATE_HOME` 优先）拿服务地址与密码，用 Basic auth（用户 `opencode`）长连 `GET /api/event`，解析 SSE 的 `data:` 行维护各会话状态；断线 2 秒、服务未运行 5 秒后重连，端口变化自动跟上。**子 agent 的子会话不上面板**：会话列表里带 `parentID` 的、`session.created` 事件里带 `parentID` 的（V2 直接放在事件里，V1 在 `info` 里）、以及活跃表里、列表外、单独查一次才认出 `parentID` 的，都记进 `children` 长期忽略，之后任何带这个 id 的事件（改名、工具调用、结束、提问）都不再处理——子会话跑完常被 OpenCode 删掉，事件尾巴还会飘过来几秒，只跳过创建不够；`children` 超过 300 个就丢掉最早记住的一批。通知走 `osascript`（提示文本按 AppleScript 字符串转义后拼接，`shell:false`）与 `afplay`。会话列表按**最近一次状态变更**倒序（`changedAt`/`changeSeq`，只有状态真的变了才刷新；工具调用、文本输出等活动事件不重排，列表不会来回跳），任何状态变更都会把会话顶到最前。`acked` 表示用户已经处理过该会话：点会话行（**能不能跳过去都算**——终端页已经关掉、`terminals.focus` 失败也照样置位）或对应终端在前台且正停在那个标签页（`server.mjs` 每 3 秒用 `terminals.viewed(...)` 判断后调 `acknowledge`）都会置位；**已结束 / 已终止且 `acked` 的会话不再出现在 `snapshot().sessions` 里**（列表与折叠头像一起收起，避免残留），状态再变会重新变成未查看并重新出现。设置存 `~/.bobo/opencode.json`：`notify`、`sound`、`notch`、`hideWhenIdle`、`autoExpand`、`rows`（1–8）、`menubar`（默认 false，是否有菜单栏图标）、`movable`（默认 false，刘海是否可拖）、`display`（`auto`/`builtin`/`main`，面板在哪块屏）、`quotaView`（`cycle` 点圆环切换来源 / `expand` 展开时并排显示各来源，默认 `expand`）、`quotaCount`（并排最多几家：3 / 5 / 7，0 = 自适应，默认 0）。加设置项时三处要同步：这里的默认值与 `loadSettings`/`saveSettings`、Swift `IslandSettings` 的解码、网页的气泡开关。
- `codex.mjs`：「通知岛」里 Codex CLI 会话的数据层。Codex 没有事件流服务，改为每 2 秒轮询 `~/.codex/sessions/<年>/<月>/<日>/rollout-*.jsonl`（只扫最近两天、按 mtime + size 判断变化，只读文件头尾）；从 `session_meta` 拿 session_id 与 cwd，从尾部 `event_msg` 推导状态：`task_started`→运行中、`task_complete`→已结束、`turn_aborted`→已终止（若出现审批类事件则→等你回答）。**子 agent 的 rollout 不上面板**：`session_meta` 里 `thread_source: subagent`（或 `source: {subagent: …}`）的文件直接跳过，只有父会话自己的线程照常收录，否则一次任务会多出一串重复的头像。**同一会话的派生 rollout 文件只让最新写入的那份定状态**：Codex 桌面版 rollover / fork 会另写一份 `<原会话id>_<新会话id>.jsonl`，里面的 `session_meta.session_id` 仍是原 id，两份文件的尾部状态还不一样（老文件停在 `turn_aborted`、续写文件走到 `task_complete`）——逐个文件 update 会让同一个会话每轮轮询在两种状态间来回翻、提醒每 2 秒响一次，所以状态取最新写入的那份（mtime，同刻比 `lastAt`），开始时间取同组里最早的一份（计时对得上整个线程），其余文件只用来算「会话还在」。标题读 `~/.codex/session_index.jsonl` 的 `thread_name`。`session_meta` 的 `originator` / `source` 还区分这一轮是谁跑的：`Codex Desktop`、`codex_work_desktop`、`source: vscode` / `appserver` 是 **Codex 桌面版（Codex app / ChatGPT.app，bundle id `com.openai.codex`）**，`codex-tui`、`codex_cli_rs`、`source: cli` / `exec` 是 CLI——`isDesktopOrigin` 是导出的纯函数，会话因此带一个 `app` 标记（`appSession(id)` 给 `server.mjs` 用），点会话时优先跳 app 的线程深链（见 `terminals.mjs`）。**这份 meta 只在会话头写一次，记的是线程的出身，不是「现在谁在跑它」**：CLI `codex resume` 一个 app 里起的线程时会接着往同一份 rollout 追加，头部照旧写着 `Codex Desktop`——所以 `app` 只当「优先跳 app」的信号，不当作「一定不是终端」；反过来 app 里跑的会话也可能记成 `codex-tui`（同一条 rollout 的 originator 与谁在跑并不总是一致）。结束 / 终止挂起 1500ms 再提醒（复用 `opencode.remind`，受通知岛开关控制），有新活动就撤销；结束 / 终止的会话保留 30 分钟，被 `acked`（点过会话行或终端里停过）后不再出现在列表与头像里。会话 id 统一加 `codex:` 前缀，`source` 字段区分来源；`server.mjs` 把六个 Agent 的会话合并进状态流（各自给 `order`，按最近一次状态变更倒序），并让终端的「看过了」轮询同时覆盖 Codex、omp、Claude Code、dsh 与 agy。
- `omp.mjs`：「通知岛」里 omp（Oh My Pi）会话的数据层。omp 也没有事件流服务，改为每 2 秒轮询 `~/.omp/agent/sessions/<目录编码>/<时间>_<会话ID>.jsonl`（`PI_CODING_AGENT_DIR` 优先；只扫最近 30 分钟有写入的，按 mtime + size 判断变化，只读头尾）：第一行是定宽标题槽（改标题原地重写，解析时取 `title`），header 拿会话 id 与 cwd；**流式文本要等整条消息完成才落盘**，所以尾部 entry 能稳定标出状态——`session_exit` 记录退出（`kind: normal` → 已结束，signal / fatal / process_exit → 已终止；没有它说明进程还活着），assistant 不带工具调用地收尾 → 已结束，user / toolResult / bashExecution / pythonExecution / `custom tool_execution_start` → 运行中，其中 `ask` 工具本身就是等你回答；assistant 带工具调用但超过 2 秒还没写出 `tool_execution_start`（等你批准）也算等回答。结束 / 终止挂起 1500ms 再提醒（复用 `opencode.remind`），有新活动就撤销；结束 / 终止保留 30 分钟，被 `acked`（点过会话行或终端里停过）后不再出现在列表与头像里。文件被原子重写时可能瞬间读不到，连续 3 轮扫不到才认为会话真的消失（`MISS_LIMIT`），解析失败的一轮沿用缓存里的状态而不再清会话。会话 id 统一加 `omp:` 前缀，`source: 'omp'`；`GET /api/opencode` 的快照里另有 `omp` 字段。
- `claude.mjs`：「通知岛」里 Claude Code 会话的数据层。Claude Code 没有对外的事件流，但有两份可读信号：`~/.claude/sessions/<pid>.json` 是活跃会话的注册表（进程退出即删），`status` 取 `idle` / `busy` / `waiting`，`waiting` 时 `waitingFor` 说明在等什么（`permission prompt` 权限确认 / `input needed` 要你回答）——**实时状态以它为准**（只认 `kind: 'interactive'` 且 pid 还活着的，用 `process.kill(pid,0)` 兜底 kill -9 留下的残骸）；`~/.claude/projects/<目录编码>/<会话ID>.jsonl` 是会话日志，尾部有 `ai-title`（AI 生成的标题）与 `last-prompt`，用来补标题与 cwd（只读头尾、按 mtime + size 缓存）。状态映射 `busy → 运行中`、`waiting → 等你回答`（detail 用 `WAIT_HINTS`，未知 `waitingFor` 原样显示）、`idle → 已结束`，认不出的中间态按运行中，`statusUpdatedAt` 超过 15 分钟仍停在运行中的按结束处理；进程退出时还在跑或等回答的按已终止落定（落定后加 `gone` 标记，不再反复触发）。进入「等待」挂起 800ms、结束 / 终止挂起 1500ms 再提醒（复用 `opencode.remind`）；结束 / 终止保留 30 分钟，被 `acked`（点过会话行或终端里停过）后不再出现在列表与头像里。会话 id 统一加 `claude:` 前缀，`source: 'claude'`；`GET /api/opencode` 的快照里另有 `claude` 字段。标题是 `✳ <标题>`（Otty / Ghostty / Terminal 都按这个前缀匹配）。
- `dsh.mjs`：「通知岛」里 DeepSeek Harness（`dsh` 命令，默认 `~/.dsh`，`DSH_HOME` 优先）会话的数据层。dsh 把每个会话的**投影缓存**写在 `~/.dsh/storages/session_projcache/sessions/<会话ID>.json`（version 7，百 KB 级、活动时持续重写），bobo 每 2 秒按 mtime + size 缓存后整份解析：`record.identity.cwd` 是目录，`record.rows.title.val` 是 AI 标题（缺失时退回 `turnOutline` 最后一轮的 prompt），`turnBoundary.val.openTurnStartSeq` 或 `sessionStats.val.openStep` 非空 → 运行中，`notification.val.last.reason` 的 `completed` → 已结束、`aborted` / `interrupted` / `failed` / `error` / `crashed` → 已终止，`plan.val.wanted` 非空（模型请求进 plan 模式）→ 等你回答。**审批与 `ask_user` 的等待不写投影行**，所以那两种等待仍显示成运行中（拿不到信号就不猜）。`at` 用文件 mtime（投影每次活动都重写，比 `sessionListMetadata.lastPromptAt` 准），超过 15 分钟没更新还停在运行中的按结束处理；读取撞上半份文件时沿用上一轮状态、不当作会话消失。结束 / 终止挂起 1500ms、等回答挂起 800ms 再提醒（复用 `opencode.remind`）；结束 / 终止保留 30 分钟，被 `acked` 后不再出现在列表与头像里。会话 id 统一加 `dsh:` 前缀（文件名里的 `session-` 前缀会剥掉），`source: 'dsh'`；`GET /api/opencode` 的快照里另有 `dsh` 字段。dsh 的 web profile 没有终端标题集成，所以**不加标题前缀**。**点 dsh 会话的跳转也不同**：`server.mjs` 的 focus 路由先用 `dshWebAlive()`（HEAD `http://127.0.0.1:3080/`，端口可被 `DSH_WEB_PORT` 覆盖，dsh 默认 3080）探测 web profile 是否在跑——在跑就交给 `browser.focus()` 把用户带过去（**先切到浏览器里已经打开的那个标签页，没有才用默认浏览器打开**；dsh 前端没有会话级深链，只能回到首页让用户自己选会话），否则再走 `terminals.focus`（tui profile 仍按标题 / 目录兜底匹配）；两种情况都会 `dsh.acknowledge`（点击即收起）。
- `agy.mjs`：「通知岛」里 Google Antigravity（`agy` 命令，默认 `~/.gemini/antigravity-cli`）会话的数据层。agy 把所有会话概要维护在 SQLite 库 `~/.gemini/antigravity-cli/conversation_summaries.db` 的 `conversation_summaries` 表（**只读**，用内置 `node:sqlite` 打开：没有 `-wal/-shm` 时用 `immutable=1` URI 直读，绝不新建文件），每 2 秒轮询最近 40 行推导状态：`status = CASCADE_RUN_STATUS_RUNNING` 或 `not_fully_idle = 1` → 运行中（超过 `STALE_MS` 15 分钟没更新就按已结束处理，免得中断留下永远转圈的头像），`killed = 1` → 已终止，其余 → 已结束；运行中的会话再读一次 `brain/<会话 ID>/.system_generated/logs/transcript.jsonl` 的末尾，最新一步是带 `ask_question` 的 `PLANNER_RESPONSE` 时算等你回答（审批类等待不写这里，所以那种等待显示成运行中）。标题取 `title`（退回 `preview`），目录取 `workspace_uris` 里第一个 `file://` 路径，`startedAt` 取 `last_user_input_time`（退回 `last_modified_time`）。**子代理会话不上面板**：`parent_conversation_id` 非空或 `nesting_depth > 0` 的直接在 SQL 里过滤；连续 3 轮扫不到才认为会话消失（`MISS_LIMIT`），读取撞上重写中的库就沿用上一轮状态。会话 id 统一加 `agy:` 前缀，`source: 'agy'`；`GET /api/opencode` 的快照里另有 `agy` 字段（含 `available` / `reason`：运行时读不了 SQLite（Node < 22.13）时网页那一栏直接显示原因，而不是装作「没有会话」）。标题按 `AGY | <标题>` 或 `Antigravity | <标题>` 匹配终端标签页。
- 「通知岛」的事件名是 V2 的公开事件流：`session.inbox.enqueued` / `session.execution.started|succeeded|failed|interrupted` / `form.created|replied|cancelled` / `session.renamed` / `session.created` / `permission.asked|replied`。提问走的是 **form**（不是 `question.asked`，那是 V1），所以「等你回答」必须认 `form.created`；`permission.asked` 先挂起 0.8 秒（`askPermission`）再落定成「等你回答」并提醒——auto 模式会自动批准，随后到达的 `permission.replied` 取消挂起，不该为这种不用回答的询问闪状态、弹提醒；落定后 `permission.replied` 把状态收回「运行中」；`session.status`(idle/busy) 在事件流里不可靠，结束/终止一律以 `session.execution.*` 为准。
- 「通知岛」是第三个一级视图（`index.html` 的 `islandTab` + `islandWorkspace`，`app.js` 的 `switchView`/`renderIsland`）。`switchView` 用 `islandView` 记当前视图，进入时订阅 `/api/opencode/stream`、离开时断开（`openIsland`/`islandClose`）；`agentActive` 只是「是否在智能体视图」的派生值，⌘S 保存逻辑依赖它，别删。顶栏不再放视图专属操作：技能相关入口在技能视图自己的左栏（`#add`「＋ 添加技能」，与智能体视图的「＋ 新建智能体」同构）和文件夹视图里；顶栏右侧常驻的是 GitHub 入口（`#repoLink`）与版本按钮（`#appVersion`）——版本按钮同时是**更新入口**：`renderVersion` 按 `/api/app` 的状态改文案与类名（`is-busy` 检查/下载中带百分比、`is-ready` 已就绪呼吸、`is-error` 失败变红），点击时「已就绪 → 重启安装（确认框带更新说明）、其余 → 检查更新」；`startAppPolling` 每 2 秒轮询一次（页面隐藏时停）。通知岛本身也是「左侧分类 + 右侧分区」：侧栏用 `settings-menu` 分两组——**通知岛设置**（`general` 一个分栏，里面用 `h3` 分成提醒 / 刘海面板 / 内容 / 菜单栏与位置四块）与 **Agent 连接**（`opencode`、`codex`、`omp`、`claude`、`dsh` 与 `agy` 六个分栏，条目右侧的 `#islandNavDot`/`#islandCodexDot`/`#islandOmpDot`/`#islandClaudeDot`/`#islandDshDot`/`#islandAgyDot` 跟着各自最新会话状态变色，`renderIsland` 里更新）；`showIslandPane` 只切分栏显隐，`renderIslandSettings` 在分栏里渲染开关（行 id 还是 `rowNotify`/`rowNotch`/`rowMenubar`/…；「额度查看方式」与「并排显示数量」是两个下拉 `#quotaViewSelect` / `#quotaCountSelect`，后者只在「展开并排」时可用），**会话列表按来源分开放在六个分栏里**（OpenCode 用 `#islandList`/`#islandCount`，Codex 用 `#codexList`/`#codexCount`，omp 用 `#ompList`/`#ompCount`，Claude Code 用 `#claudeList`/`#claudeCount`，DeepSeek 用 `#dshList`/`#dshCount`，Antigravity 用 `#agyList`/`#agyCount`，都由 `renderIsland` 从合并后的 `sessions` 里按 `source` 过滤）。左侧底栏只留来源说明（原来的「设置」按钮已删除，因为设置是常驻 tab）。`openIsland` 打开时还会每 4 秒 `POST /api/terminals/watch` 让服务端续期终端归属扫描（`islandClose` 停），会话行第二行因此能显示 `OpenCode · Terminal · 目录`；归属只在这时算，关掉视图服务端就停扫。会话行右侧还有一格**计时**（`elapsedText`）：显示会话开始至今的时长（`<1m` / `5m` / `2h` / `1d`，口径对齐 CodeIsland 的 SessionTag），`fillSessionRow` 写文字、`refreshSessionTimes` 在视图打开期间每秒只改这一格文字（`islandClock` 定时器，`document.hidden` 时不刷，离开视图即停），不重建行、也不依赖状态流推快照。
- 「用量」是第四个一级视图（`index.html` 的 `usageTab` + `usageWorkspace`，`app.js` 的 `openUsage`/`usageClose`，进入时 `loadUsage` 并用 60 秒定时器刷新）。侧栏是 `settings-menu` 的来源分栏（`data-usage-pane` 的 Codex / OpenCode Go / Antigravity，条目右侧 `#usageDotCodex`/`#usageDotOpencode`/`#usageDotAgy` 表示是否可用，点分栏会同时 `POST /api/usage/provider` 把刘海胶囊切到这家）；「刷新额度」是右下角的悬浮按钮（`#usageRefresh`，与命令行凸起同位置，只在用量视图显示，不是侧栏菜单项）。右栏每个来源顶部有一条启停开关（`#rowUsageCodex`/`#rowUsageOpencode`/`#rowUsageAgy` → `POST /api/usage/enabled`，关掉的来源不参与刘海胶囊的点击循环，数据照常读取）；只有 OpenCode Go 分栏有 API Key 表单（`#usageKeyForm`，保存前先调接口验证、清除后回退到环境变量/本机登录，页面不回显 Key），它还有 `#usageDays`（最近 30 天）与 `#usageModels`；Antigravity 分栏用 `#usageAgyWindows` 列窗口、`#usageAgyNote` 说明数据来自哪条通道（`keySource` 是 `local-server` 还是 `cli`）。窗口卡片用 `usage-grid`/`usage-card`/`meter`，`usageRow` 用 `.usage-day` 网格（日期/条形/数值），模型行加 `.model`。
- 「设备」是第五个一级视图（`index.html` 的 `deviceTab` + `deviceWorkspace`，`app.js` 的 `openDevice`/`deviceClose`，进入时每 2 秒拉一次 `/api/devices`，离开就停；右下角悬浮的「刷新设备」`#deviceRefresh` 走 `?refresh=1` 立刻全量采一次）。侧栏是 `settings-menu` 的三项分栏（`data-device-pane` 的 CPU / 内存 / 磁盘，行尾 `.device-value` 是实时数值 + `data-level` 变色）；右侧 `settings-pane` 用「用量」那套 `usage-grid`/`usage-card`/`meter` 渲染大卡（内存 / 磁盘各一张，CPU 是占用率 + 负载均值两张）+ `settings-card` 明细，CPU 分栏下面还有 `#deviceCores` 的每核占用条（`.core-grid`），CPU 与内存分栏末尾各有一份 Top 进程表（`#deviceProcesses` / `#deviceMemoryProcesses`，`.process-row`，bobo 自己的进程钉在最前，按需轮询见 `devices.mjs`）。容量单位照 macOS 习惯：内存用 About This Mac 的口径（GiB 记作 GB）、磁盘用 Finder 的十进制，`deviceGB` 的第二个参数区分。刘海胶囊里额度右边的设备指示（见下一条）点击会 `evaluateJavaScript` 点 `#deviceTab` 打开这个视图。
- 「设置」是常驻一级 tab（顶栏最后一个，`#settingsTab`；原来从侧栏左下角按钮打开的临时 tab 与 × 关闭按钮都已删除，`.open-settings` 不再存在）。`settingsWorkspace` 是「左侧分类导航 + 右侧分区卡片」：左边 `settings-nav`（`data-settings-pane` 的外观 / AI / 关于，`showSettingsPane` 只切面板显隐），右边 `settings-pane` 里是 `settings-card`/`settings-row`（标签左、控件右；窄控件加 `class="narrow"`）。**刘海与菜单栏设置不在这里**：`notch`/`autoExpand`/`hideWhenIdle`/`rows`/`menubar`/`movable`/`display` 都在「通知岛」视图（见上一条）。进入设置时 `switchView` 只调 `loadAISettings()`。**「关于」页是 app 信息 + 开源社区两块**（更新入口不在设置里，见下一条）：`about-hero` 显示图标、版本（`#aboutVersion`）、构建号（`#aboutBuild`）与作者 / 许可；开源社区卡片打开仓库 / Releases / Issue（都走 `api('open',{url})` 交给系统浏览器）。AI 表单的 id 没变（`aiSettingsForm`）。`#console`/`#consoleHandle` 只在技能 / 智能体视图显示。
- `terminals.mjs`：点击会话跳到对应终端。按 **Otty → Ghostty → Terminal.app** 的顺序试，只对「已安装且正在运行」的适配器尝试（会话在哪个终端，那个终端一定开着），命中就返回；都没命中就激活正在运行的那个（都不在跑就打开第一个已安装的）。对上层保持与旧 `createOtty` 相同的 `{focus,focusApp,viewed,available}` 接口，`server.mjs` 的 `ackViewed` 用 `terminals.viewed([...opencode.unviewed(),...codex.unviewed(),...omp.unviewed(),...claude.unviewed(),...dsh.unviewed(),...agy.unviewed()])`、`/api/opencode/focus` 调 `terminals.focus`（按会话 id 前缀区分 `codex:` / `omp:` / `claude:` / `dsh:` / `agy:`）。**Codex 桌面版不算终端**：`focusApp` 用 `/usr/bin/open` 打开线程深链 `codex://threads/<会话 id>`（`codexThreadUrl` 会剥掉 `codex:` 前缀并转义；`open` 返回非 0 说明装不上，继续走终端匹配）——**只在 app 正在跑时调用**，否则就是把已经退出的 app 拉起来。`focus` 里 app 出身的会话（`opts.app`，由 `server.mjs` 按 `codex.appSession(id)` 传入）先按标题在终端里认一遍（`matchTab` 的 `{titleOnly:true}`：Codex CLI 的标签标题是 `<线程名> | <项目>`，CLI resume 过的 app 线程因此认得出来；同目录不算数，否则会跳到同目录里另一条无关的标签页），认到了就跳那个标签页；认不到、而 Codex app 正在跑时才跳深链，最后才激活 / 打开终端（app 没跑也不跳深链，免得把退出的 app 拉起来）。CLI 会话照旧先找终端标签页，三个终端一个都没匹配到、而 Codex app 正在跑时也退到深链（app 里跑的会话也可能记成 `codex-tui`）。Ghostty 走 AppleScript（`tell application "Ghostty"`，读 `terminals` 的 `id`/`name`/`working directory`，`focus (terminal id …)`，旧版不支持时退回按工作目录找），Terminal.app 也走 AppleScript（遍历 `windows`/`tabs` 读窗口 `id`、`tty`、`custom title`，按窗口 id + 标签序号选中；Terminal 的 tab 没有 `name`，只能靠 `custom title`）。会话只有目录与标题（没有 tty）：匹配顺序是**工作目录精确相等 → 标题等于 `<前缀><会话标题>` → 标题包含会话标题 → 标题包含目录名**，`viewedIds` 用 strict 模式（不做最后一步）避免同名目录误判成「看过了」。标题前缀与 Otty 相同（OpenCode `OC | `，omp `π `——运行中前缀后多一个 spinner，Claude Code `✳ `，`sameTabTitle` 会跳过前缀后的非文字字符再比较，Codex 依次 `CX | `、`Codex | `、`CDX | `，agy 依次 `AGY | `、`Antigravity | `）。前台判断用 `lsappinfo front` 的 bundleID，正在运行用 `lsappinfo list` 的 bundleID 集合；`osascript`/`lsappinfo` 都用绝对路径、参数数组 + `shell:false`。「看过了」对三个终端同一套规则：只有该终端在前台、且正停在会话的标签页上才算；Otty 还在 `activeOttyTabs` 里再收一层——多窗口时只认**聚焦窗口**里 `active` 的那个标签页，别的窗口里「选中的标签页」不算（用户并没有在看）。`parseGhosttyTabs`/`parseTerminalTabs`/`parseTerminalActive`/`matchTab`/`viewedIds`/`activeOttyTabs`/`locateSessions`/`locateAppSessions`/`codexThreadUrl`/`escapeAppleScript`/`samePath` 都是导出的纯函数，`terminals.test.mjs` 单独测试。`locate(sessions)` 用 `locateSessions(sessions,groups)` 给每个会话算归属终端（按适配器优先级取第一个命中，`{会话 id: 终端名}`），只扫「正在运行的」终端；`app` 标记的会话与 `focus` 同一套规则——先按标题认终端（`titleOnly`），认不到的、app 在跑才由 `locateAppSessions` 归给 `Codex App`（点击走深链），app 没跑就退回普通匹配，归属显示与点击目标始终一致。终端归属不常驻扫描：网页打开通知岛、刘海面板展开时用 `POST /api/terminals/watch` 续期（10 秒窗口），`server.mjs` 在这段时间内每 3 秒 `locate` 一次并把结果并进状态流的 `sessions[].terminal`；没人续期就自然停扫，避免常驻子进程。
- `browser.mjs`：「把用户带到一个已经打开的网页」（目前只有 dsh 的 web界面用它）。`createBrowser().focus(url)` 先在各**正在运行**的浏览器里找已经打开这个 URL 的标签页：Chromium 系（`Google Chrome` / `Microsoft Edge` / `Brave Browser` / `Vivaldi` / `Chromium`）用 `windows`/`tabs`/`URL` 字典，Safari 用 `current tab` 选中，命中就 `set active tab index`（Safari 是 `set current tab`）+ 窗口置前 + `activate`，返回 `{ok:true,focused:true,app}`；带 / 不带尾斜杠都算同一个 URL（`u` 与 `b` 两个候选）。都没命中才 `open url`（系统默认浏览器，Chromium 系遇到同一 URL 通常也会复用已有标签），返回 `{focused:false,opened:true}`。Arc 等自成一系的 AppleScript 不支持这组字典，故意不列；只读标签、只切标签，不改浏览器配置。`chromiumFocusScript` / `safariFocusScript` / `escapeAppleScript` 是导出的纯函数，`browser.test.mjs` 只测脚本生成与转义，不真调 `osascript`。
- `update.mjs`（应用内更新）：`createUpdate({home})` 读 `package.json` 的版本、从启动路径定位 `.app`（`appBundlePath`，认不出就是源码运行、`canUpdate=false`）；启动约 15 秒后静默检查 GitHub Release（`releaseApi`，无 token 的 60 次/小时足够），有新版就自动下载 `bobo.app.zip`、校验 sha256、用 `ditto -x -k` 解压到 `~/.bobo/updates/staged/`，并写 `staged.json`（重启后按它恢复「已就绪」；版本不再新就清掉残留）。只有非草稿、非预发布（`/releases/latest` 本来也只会返回非预发布）、版本比当前新、带 `bobo.app.zip` 且资产有 `sha256` digest 的 release 才会被接受（`releaseCandidate`）——所以 beta 轮次按普通 Release 发布（别加 `--prerelease`），版本号里的 `-beta.N` 后缀只作展示与版本比较。`install()` 生成 `relaunchScript()`（纯函数）交给 detached 的 `/bin/sh` 执行，参数依次是日志、目标 app、暂存 app、更新目录与应用 PID：先 `osascript` 请应用自己退出（能触发 `applicationWillTerminate` 收尾），1 秒内没退就直接 `kill -TERM $APP_PID`、再 3 秒 `kill -9`（`osascript` 受「自动化」权限限制，不能只靠它，这是「点了重启像没反应」的教训）；然后等 4318 释放、`xattr -cr` 后重命名替换（失败回滚旧包）、`open` 并验证进程，全程写 `~/.bobo/updates/relaunch.log`。`install()` 前先确认暂存包还在（不在就转 `failed` 让用户重新检查，而不是卡在「正在重启」），spawn 后挂 40 秒看门狗：应用没退出就把状态恢复成 `ready` 供重试。解压后除了校验 bundle id 与版本，还要求 `codesign --verify --deep --strict` 通过且 `TeamIdentifier` 等于 `teamId`（`LGKLTGNTY2`，与发布流水线一致），签名不对的包直接拒绝安装。接口：`GET /api/app`、`GET /api/update`（同一份快照）、`POST /api/update/check`、`POST /api/update/install`。源码运行不自动检查（`start()` 里判断 `canUpdate`）、也拒绝安装；`compareVersions` / `parseVersion` / `releaseCandidate` / `appBundlePath` / `plistValue` / `relaunchScript` / `readPackage` 是导出的纯函数，`test/update.test.mjs` 注入 fetch / ditto / spawn 测试全流程，不联网、不替换真实应用。
- `otty.mjs`：`terminals.mjs` 里的 Otty 连接层，只负责跑 otty-cli：`tab list --json`（标签页，含 `cwd` / `window_id` / `active`）、`window list --json`（窗口与哪个聚焦）、`tab focus <id>` 并在成功后 `open /Applications/Otty.app` 激活；**找不到只回报失败**（由 `terminals.mjs` 决定后续：换别的终端或打开某个终端）。会话与标签页的匹配（含各 Agent 的标题前缀）、以及「Otty 在前台且正停在会话标签页上」的判断都在 `terminals.mjs`（与「会话归属哪个终端」共用同一套 `matchTab`），这里不再复制一份前缀表——两边各改各的会导致某个来源在 Otty 里永远不算「看过」。`otty-cli` 固定读 `/Applications/Otty.app/Contents/MacOS/otty-cli`，参数用数组 + `shell:false`；`server.mjs` 每 3 秒把六个 Agent 的未查看会话合并后轮询一次（没有未查看的会话时直接返回，不落子进程）。网页和通知岛面板都走 `POST /api/opencode/focus`（`Bobo.swift` 的 `focusIsland` 用 `apiToken` 调同一个接口），跳转成功也会顺带标记已查看。
- `devices.mjs`：设备（CPU / 内存 / 磁盘）。CPU 用 `os.cpus()` 的累计时间差算 2 秒窗口的整机占用 + 每核占用，`os.loadavg()` 给 1/5/15 分钟负载；内存解析 `/usr/bin/vm_stat`（`exec` 数组参数 + `shell:false`，退出码非 0 视为失败）的页统计，已用 = 活跃 + 联动 + 压缩占用 − 可回收、缓存 = 非活跃页，压力等级读 `kern.memorystatus_vm_pressure_level`（1/2/4 → 正常/警告/紧张，警告会抬高等级）；磁盘用 `fs.statfs('/')`（APFS 容器级，与 Finder 一致）。**不要用 `os.freemem()` 算内存**：macOS 上它只报 free 页（本机常年 0.09 GB），会把内存显示成 99%。非 macOS 平台退回 `os.freemem()`，测试环境才不会炸。采样在服务端常驻：2 秒一跳，内存每 3 跳、磁盘每 30 跳，`createDevices({now,cpus,loadavg,totalmem,freemem,statfs,exec,platform})` 全部可注入以便测试（`devices.test.mjs` 不读真实系统、不起子进程）。快照：`{available,updatedAt,cpu:{usage,perCore,cores,model,load,level},memory:{total,used,cached,free,wired,compressed,pressure,pressureLabel,usedPercent,level},disk:{mount,total,used,free,usedPercent,level},error}`；等级 `ok/warn/low`（阈值 CPU 50/80、内存 70/85、磁盘 80/90，内存还要看压力）。去重按「看得见的变化」：CPU 取 5% 粒度、内存与磁盘取 0.1%，空闲时不刷流；`updatedAt` 不参与。挂在 `/api/opencode/stream` 快照的 `device` 字段里，`GET /api/devices`（可加 `?refresh=1`）给网页。`?refresh=1` 撞上常驻跳时不会复用那一跳的快照：等它收尾后再补一次全量采样，否则网页「刷新设备」/ 通知岛明细卡可能拿回几十秒前的磁盘值。另外 `processes({limit,sort})` 给「设备」的 CPU / 内存分栏提供 Top 进程：只读 `/bin/ps -Ao pid=,pcpu=,pmem=,rss=,etime=,comm=`（一次 fork），按 `cpu` 或 `memory`（RSS）排序取前 15（上限 50），结果缓存 1.5 秒合并重复请求，`parsePs` 是导出的纯函数（basename + KB→字节）。bobo 自己的进程（`pid === process.pid` 的服务进程，或名字为 `bobo`/`Bobo` 的 `bobo.app`，大小写不敏感）标记 `self` 并钉在列表最前（服务在前），**不占**前 15 条的名额——前端把它显示成「bobo」加「服务」/「应用」小标签。它**不**进常驻 tick、也**不**进状态流——只在网页停在 CPU / 内存分栏且前台时由 `GET /api/devices/processes` 触发（app.js 的 `processTimer`，3 秒一次，切走 / 切后台即停），这是这块低功耗的关键。
- `usage.mjs`：用量（Codex + OpenCode Go + Antigravity 三家）。Codex 读 `~/.codex/auth.json`（`CODEX_HOME` 优先）的 OAuth token 调 `https://chatgpt.com/backend-api/wham/usage`，窗口按 `limit_window_seconds` 归类（18000 = 5 小时、604800 = 一周，与 CodexBar 的归一化一致），**只读凭据：不刷新 token、不写回**；登录过期提示在终端跑一次 `codex`，只有 API Key 时说明额度需要 ChatGPT 订阅。OpenCode Go **API 优先**：key 取 `OPENCODE_API_KEY`（环境变量，CodexBar 的顺序）或 `~/.local/share/opencode/auth.json` 里 opencode-go 的 key，调 `https://opencode.ai/zen/go/v1/usage`，`usage.{rolling,weekly,monthly}.percent` 是「已用百分比」、`resetsAt` 是 ISO 时间、`status=rate-limited` 表示已限额（与 CodexBar 一致）；接口失败或没有 key 时退回本机估算：只读 `~/.local/share/opencode/opencode.db`（没有 `-wal/-shm` 时 `immutable=1` 直读，绝不新建 sidecar），取 `opencode-go` 的步骤费用（`step-finish` 分片优先、消息级兜底），按 12 / 30 / 60 美元折算 5 小时 / UTC 周 / 账单月（账单月用最早一条本机记录锚定），并在 `error` 里说明降级原因。Antigravity（agy）**先试本地 Language Server**（Connect-RPC 接口 `RetrieveUserQuotaSummary`，地址与 CSRF token 从 `ANTIGRAVITY_LS_ADDRESS` / `ANTIGRAVITY_CSRF_TOKEN` 或 `~/.bobo/agy-auth.json` 缓存取——不扫进程，「语言服务在跑」本身发现不到，没给这两个值就跳过；token 还兜底从最近会话 step 的 `output.txt` 里抓），失败或没配置就退回 **agy CLI**：`agy -p /usage --output-format json --print-timeout 20s`（与 CodexBar 的同名调用一致，二进制按 `ANTIGRAVITY_CLI_PATH` / `ANTIGRAVITY_AGENTAPI_EXE` / `~/.local/bin/agy` / PATH 顺序找）。两条通道都返回各模型组（如 Gemini、Claude/GPT）的 5 小时滚动与周配额窗口，`keySource` 记 `local-server` / `cli`，界面的说明文案跟着变。快照：`{available,selected,providers:[{id,name,symbol,available,enabled,range,keySource,keyHint,reason,error,plan,credits,source,estimated,windows:[{key,label,status,usedUSD,limitUSD,usedPercent,remainingPercent,resetInSec,resetsAt}],daily,models,totals}]}`；`source:'api'` 走网络（3 分钟节奏、手动刷新最快 60 秒一次），`source:'local'` 每 60 秒；读取失败（`available!==true`，或有 Key 但接口失败降级到本机估算）按 2 / 4 / 8 / 10 分钟递增退避（`backoff`，成功后清零），自动 tick 尊重退避、手动 force 不受限；`resetInSec`/`resetsAt`/`updatedAt` 不参与去重（数据没变不推送）。设置存 `~/.bobo/usage.json`（权限 0600）：`provider`（刘海胶囊显示哪家）、`enabled`（来源启停：只有「可用且开启」的才参与 `POST /api/usage/provider` 的选择与循环，关掉当前那家会自动换一家）、`ranges`（各来源圆环显示哪一档窗口：`session` / `week` / `month`，由 `POST /api/usage/range {id}` 循环写入，见 `cycleRange`；读回时认不出的档位退回 5 小时窗口、再退回第一档，每家的窗口变了也不会留个空范围）、`keys`（手动填的 Key，优先级高于环境变量与本机登录）、`notifyReset`（额度重置提醒开关，默认 true）。`POST /api/usage/enabled {id,enabled}` 启停来源；`POST /api/usage/key {key}` 保存前先调一次接口验证（失败不落盘、返回 `{ok:false,message}`），`{clear:true}` 清除；换 Key / 清除后都先 `cache.delete(id)` 再重读，避免吃旧缓存。`POST /api/usage/reset-notify {enabled}` 开关额度重置提醒（`usage.setNotifyReset`）；检测在 `detectResets`：Codex 与 Antigravity 的窗口从剩余 < 100% 回到 100%（前后两次都必须是有效读数）时通过注入的 `notify`（server.mjs 传 `opencode.remind`）走通知岛通道发 `reset` 提醒（音效 `Hero.aiff`，受通知岛的系统通知 / 提示音开关约束）。`GET /api/usage?refresh=1` 强制刷新，但仍受硬下限保护。用量挂在 `/api/opencode/stream` 快照的 `usage` 字段里，原生不用另外轮询。
- 网页与原生之间的通道：macOS 端没有 JS 桥——网页与 `Bobo.swift` 都通过本机 HTTP 服务交互（网页走 `/api/*`；原生订阅 `/api/opencode/stream`，并用 `postIsland` 调 `/api/opencode/focus`、`/api/terminals/watch`，通知岛面板是 SwiftUI 原生绘制的）。v2（Tauri 壳）的面板页 `public/panel.js` 用 `window.__TAURI__` 调 `island_resize` 回报窗口尺寸（见 `v2/src-tauri/src/island.rs`），其余数据仍走 HTTP。
- `public/markdown.js`、`public/stream.js`、`public/agent-format.js`、`public/codex-format.js` 同时被服务端代码和浏览器代码 import（`reader-ai.mjs` / `ai.mjs` / `agents.mjs` / `codex-agents.mjs` 引用 `public/` 下模块）。`splitMarkdown` 的分段 ID 同时决定翻译对齐与阅读渲染，修改后两端都要验证。`renderBlock` 依赖 `document`，只能在浏览器使用；`frontmatterRows` 是不依赖 DOM 的纯函数（声明块表格的解析），可直接单元测试。`agent-format.js` 的 frontmatter 解析与模板同时决定列表展示、校验与新建内容。
- `server.mjs` 的静态资源是硬编码白名单（`/`、`/app.js`、`/style.css`、`/markdown.js`、`/stream.js`、`/agent-format.js`、`/codex-format.js`、`/panel.html`、`/panel.css`、`/panel.js`，从模块上一级的 `../public` 读取，所以 App 包里 `Resources/src/server.mjs` 与 `Resources/public/` 并存）；新增 `public/` 文件必须同时注册，否则 404。CSP 禁止内联脚本（所以样式都放在 `style.css` 里，页面不能带 `<style>`）。`/` 与 `/panel.html` 会替换 `__TOKEN__`。`panel.*` 是通知岛面板页（给跨平台桌面壳的透明悬浮窗用，浏览器直接访问也能看）。
- 顶栏 tab 与设置页分类的图标是 SF Symbols：`index.html` 顶部内联了一个 `.sf-sprite`（`sf-stack` 技能 / `sf-person2` 智能体 / `sf-bell` 通知岛 / `sf-gauge` 用量 / `sf-gearshape` 设置，取自 Apple SF Symbols 的 SVG 导出，路径里已把 black 换成 `currentColor`；顶栏右侧另有一枚手写的 `sf-github`（24×24 的官方 mark，给仓库入口用）；设置页分类导航另有三个手写的 16×16 小图标 `sf-circle-lefthalf` 外观 / `sf-sparkles` AI / `sf-info` 关于，通知岛侧栏也用同样手写的小图标 `sf-notch` 刘海面板 / `sf-menubar` 菜单栏与位置，「刷新额度」用 `sf-chart`，设备视图用同样手写的 `sf-cpu` / `sf-memory` / `sf-disk`；用量视图的额度来源行与通知岛侧栏的 Agent 连接行都用真实品牌图标 `provider-codex`（Codex）/ `provider-opencode`（OpenCode 与 OpenCode Go 同一枚标记）/ `provider-omp`（omp）/ `provider-claude`（Claude Code 的星形标记）/ `provider-dsh`（DeepSeek 的官方鲸鱼）/ `provider-agy`（Antigravity 的反重力拱门），与 `macos/assets/bobo-provider-*.svg` 同源（codex / opencode 来自 CodexBar 的 MIT 资源，omp 取自 oh-my-pi 的 MIT 品牌图标，claude 是自绘的八芒星，与 Claude Code 的终端标题前缀同形，dsh 是 Simple Icons（CC0 图标库）的 deepseek 官方鲸鱼标记，agy 也是自绘；`viewBox` 裁掉了资源自带的留白——原生那边要靠它给额度圆环留边——好让图形在 14px 的菜单图标框里和 SF 图标等大），使用时写 `<svg class="sf"><use href="#sf-xxx"/></svg>`。必须内联——`<img>`/外链 SVG 拿不到 `currentColor`，图标就没法跟随深浅色和 tab 选中态。改图标尺寸只动 `style.css` 的 `.sf`。
- `macos/`：AppKit + WebKit 包装应用。`Bobo.swift` 启动 `Contents/Resources/src/server.mjs`（App 包由 `scripts/build-app.sh` 把 `package.json`、`src/`、`public/`、头像与来源图标、`node-path`、图标 icns 复制进 `Contents/Resources/`，版本与构建编号写进 Info.plist）；`./update.sh` 先构建到临时目录，再退出正在运行的旧应用、覆盖安装到 `/Applications/bobo.app` 并打开（可传路径参数）；默认安装位置下会顺带退出并移除旧包 `/Applications/Skills Manager.app`。构建按目录复制，新增服务端模块不用改脚本。Swift 端硬编码 4318 端口。
- `v2/`：跨平台桌面壳（Tauri 2 + Rust）的独立工程，面向 macOS 与 Windows，**不影响主项目**。主项目仍是唯一真源：`v2/scripts/sync-runtime.mjs` 按 `src/server.mjs` 的 import 图把服务端模块复制到 `v2/runtime/src/`（镜像主项目结构）与 `public/` 复制到 `runtime/public/`（构建产物，gitignore），并写入 `node-path`；`v2/scripts/dev.mjs` 与 `service.rs` 启动 `runtime/src/server.mjs`。壳加载本地服务页面（`http://127.0.0.1:4318/?app=1`），网页与壳之间没有 JS 桥，全走 HTTP。Rust 侧：`service.rs` 探测/拉起 Node（复用已在跑的服务，`BOBO_PORT` 可换端口），`island.rs` 用 AppKit 补透明悬浮窗的层级（`setLevel(mainMenuWindow+2)`）与 `collectionBehavior`，面板尺寸由网页回报（`island_resize` 命令）。**主项目新增服务端模块会被自动同步**（不用维护清单，`update.sh` 也不需要）。改主项目服务端后要跑 `v2/scripts/sync-runtime.mjs`（或 `npm run dev`/`build`）才会进到 v2。
- 刘海面板（`Bobo.swift` 的 `NotchPanel`/`IslandView`/`IslandModel`）：窗口是无边框 `NSPanel`（`constrainFrameRect` 直接放行，否则系统会在屏幕重排时把它压到菜单栏下方），层级 `mainMenuWindow + 2`、全空间可见、不抢焦点；**内容用 SwiftUI 画**（`IslandShape` 顶部与屏幕边齐平、底部 0.62 连续曲率圆角，折叠 12 / 展开 22，参考 CodeIsland 的 NotchPanelShape）。窗口顶部始终贴住所选屏幕顶边（`y = frame.maxY - height`），尺寸由原生按内容算：折叠是「刘海宽度 + 80」的胶囊，展开再加出卡片（每行 31pt）。
- 面板显示在哪块屏由 `chosenScreen()` 统一决定（设置 `display`）：`auto`（默认）用 `frontmostWindowBounds()`（`CGWindowListCopyWindowInfo` 的 layer 0 窗口，排除 bobo 自己）取前台应用窗口所在屏，拿不到就保持 `currentScreenSignature` 记的当前屏，再退到有刘海的屏、主屏；`builtin` 固定有刘海的屏，`main` 固定主屏。**主屏是 `NSScreen.screens[0]`（带菜单栏的那块），不能用 `NSScreen.main`**——后者是「当前键盘焦点窗口所在的屏幕」，会随焦点在显示器之间移动，表现为「选了主屏却跟着焦点跳」（外接屏当主屏时特别明显）。所有取屏处（`positionNotch`/`resizeNotch`/`updateIslandLayout`/拖动 clamp/拖动结束记偏移）都必须走 `chosenScreen()`，别再单独写「第一块刘海屏」。屏幕重判：`didChangeScreenParametersNotification` 走 `handleScreenParametersChange`（立刻 + 0.5 秒各一次，macOS 发通知时 `NSScreen.screens` 可能还没更新），`didActivateApplicationNotification` / `activeSpaceDidChangeNotification` 走 `refreshScreen()`（只在签名变化时重排）。`resizeNotch` 的「没变化就跳过」必须同时比较 `origin.y`，否则切到同宽不同高的屏时不会重贴顶边。拖动时（`islandDragging`）不换屏。
- 通知岛的数据由**原生直接订阅** `GET /api/opencode/stream`（NDJSON，`URLSession.bytes(for:).lines`），token 在主页面 `didFinish` 时用 `evaluateJavaScript` 从 `meta[name=token]` 读一次，断线 2 秒重连。`session.execution.*`、`form.created` 等事件到 `IslandModel` 后同时驱动面板与状态栏图标（`updateNotchStatus`：图标右下角状态点 + 菜单第一行）。**状态栏图标默认不创建**（只留刘海）：`applyIsland` 按快照里的 `settings.menubar` 调 `syncStatusItem`，显示时才 `statusItem(withLength:)` 并按 `statusImage(state:)` 画图标 + 状态点，关掉时 `removeStatusItem`；菜单在 `setupStatusMenu` 里只建一次（`statusItem` 是可选的，用 `statusItem?.button`）。开关在网页「通知岛 → 菜单栏与位置」，写进 `~/.bobo/opencode.json` 后经这条流回到原生。
- 「结束/终止」要**挂起确认**才能落定（`opencode.mjs` 的 `settle`/`cancelIdle`/`ACTIVE`）：收到 `session.execution.succeeded` 后等 1200ms，期间任何活动事件（step/tool/text/reasoning/inbox/compaction）都会取消判定——一次 prompt 里 OpenCode 可能先给完成信号再继续（压缩上下文、工具续跑），直接置 idle 会误报「运行结束」通知。这套判断是 Otty 集成插件的教训（见 `~/.config/opencode/plugins/otty-integration.js` 的注释）；`session.status`(busy/idle) 与 `session.idle` 在公开事件流里目前不出现，代码里仍保留兼容分支。
- 面板动画要和内容同源：**先改模型、让出一帧再动窗口**（`scheduleIslandLayout` → `updateIslandLayout`），否则窗口已经变大而 SwiftUI 还没画内容，展开过程中会出现黑色空隙。窗口尺寸用 `NSAnimationContext`（0.2s easeOut）动画，内容不做 `transition`，靠窗口逐帧变大让列表「抽出来」。折叠态高度按公式与 `IslandView` 常量严格一致：`13.5 + 行数 × 38`（每行固定 36 + 行距 2），`list` 加 `.fixedSize(vertical:)` 防止被压缩。展开列表的 `ScrollView` 必须传 `showsIndicators: false`：内容可滚动时 SwiftUI 会在尾部预留约 17pt 给滚动条，整列右侧会比左侧多出一截空隙；`.scrollIndicators(.hidden)` 只隐藏指示条、不释放这段空间。
- 悬停展开（对齐 CodeIsland 的手感）：鼠标移入立即展开、移出延迟 0.35 秒收起；`autoExpand` 打开时状态变化会让面板自动展开——「等你回答」亮 3 秒、会话进入「已结束 / 已终止」亮 2 秒（`applyIsland` 检测到新的等待或 `justFinished` 时调 `revealIslandTemporarily(seconds:)`，把 `IslandModel.autoRevealed` 置 true 并挂 `islandAutoCollapse` 定时），没有操作就收回胶囊；自动展开时，收到提醒的那一行会描一圈提醒类型的 outline（`IslandModel.highlighted`，值是触发提醒时的状态、用它取色：等你回答橙 / 已结束绿 / 已终止红，`updateHighlights` 只在新等待 / 刚结束 / 刚终止时写入），outline 和折叠态头像一样要等用户在终端看过（`acked == true`）或会话从列表消失才撤掉，所以展开收回后仍然留着；「等你回答」刚被回答时立刻撤销（`autoRevealed` 与 `hovering` 一起清掉，避免鼠标被圈在自动展开的窗口里）；鼠标移入即解除这个限制（`islandHovering(true)` 清掉 `autoRevealed` 并取消定时），之后由悬停保持展开。悬停用 `IslandHostingView` 的 `NSTrackingArea`（`.activeAlways`）自己上报，不用 SwiftUI 的 `onHover`——非激活面板上后者不可靠。点击会话行发 `POST /api/opencode/focus`（后端 `terminals.mjs` 负责跳终端），展开时还会 `postIsland("/api/terminals/watch")` 每 4 秒续期终端归属扫描（`syncTerminalWatch`，收起 / 隐藏 / 全屏让位即停，见 `updateIslandLayout`），会话行副标题因此能显示 `Codex · Ghostty · 目录`，点击胶囊打开主窗口；按住胶囊左右拖动（阈值 5pt）移动面板，偏移以**面板中心**为基准记在 `UserDefaults` 的 `islandOffsetX`，状态栏菜单有「通知岛居中」重置；`hideWhenIdle` 打开且没有活跃会话时用 `orderOut` 真正收起窗口（显示 / 收起都带淡入淡出，见 `setNotchVisible`）；前台应用**全屏**或鼠标停在菜单栏上时让位：**不是整块 orderOut**，而是 `setIslandYielding(true)` 把面板补间收拢到刘海正中那一小段（`IslandBarGeometry.yieldSize`：宽 44、高 = 菜单栏 / 刘海高度 − 6）并**淡出到完全不可见**（`animateIslandAlpha`，0.22s；`islandAlpha` 是让位时唯一合法的 alpha，换屏补间与 `setNotchVisible` 的淡入都以它为目标，否则换屏会把收着面板又淡回可见）；窗口不 orderOut 也不接收鼠标事件（`ignoresMouseEvents`），所以不挡菜单栏 / 全屏内容上的点击，回来时是同一条「尺寸补间 + 淡入」，不会硬切——判定复用取前台窗口那段逻辑，用 `CGDisplayBounds`（Quartz 坐标，按屏幕的显示器 ID 取）比窗口 bounds，避开 `NSScreen.frame` 的原点方向差异；切空间通知（含进出全屏）后立刻 + 0.4 秒各重判一次，`refreshScreen` 在屏幕签名没变时也要走一次 `updateIslandLayout`。
- 顶部栏的分区由 `IslandBarGeometry`（纯函数：`contentWidths` / `quotaChips` / `width` / `layout` / `avatars` / `yieldSize`）算好后写进 `IslandModel.barLayout`（左翼宽度 / 中间的让位 / 右翼宽度，三者之和就是面板宽度），`IslandView.bar` 只是照着摆：**没有真实刘海**（外接屏）时中间只是一个 `itemSpacing`，宽度按内容自适应（与以前完全一致）；`hasNotch` 的屏幕上中间空出「刘海宽度 + 两侧各 `notchGap`」，头像排在刘海左侧、额度与设备排在右侧、**都贴住刘海**（左组在左翼里右对齐、右组在右翼里左对齐），内容因此不会排进摄像头模组那块物理上看不见的区域。**刘海屏是「非对称布局」**：两翼各自只包住自己的内容（`barEdge + 该侧内容`，不再取两侧较大值做成等宽），面板因此左右不对称——`islandFrame` 让**让位区正中**（而不是窗口中心）对准屏幕正中，内容少的那侧不再多出一截黑边。两侧之间统一是 `Spacer(minLength: keepOut)`（刘海屏的 `keepOut` = 刘海 + 两侧各 `notchGap`，外接屏只是 `itemSpacing`）：折叠时它正好等于让位区、内容贴住刘海两侧；展开时窗口撑到 `max(刘海 + 280, 460)`，多出来的宽度被这个 Spacer 吸收——内容因此贴住面板两端、留白压在中间，与外接屏完全是同一套机制（动画中内容随窗口两端平滑外移，`barLayout` 不随展开变化）。`islandSize` 顺手按屏幕写 `avatarLimit` 与 `quotaChipLimit`，`islandFrame` 在刘海屏上忽略 `islandOffset`（`setupIslandDrag` 直接跳过拖动）：贴在真实刘海上时面板锚定刘海正中，拖动偏移只在外接屏生效，值仍然记着。
- 折叠态显示会话头像（`IslandModel.avatars`，由 `avatarPlan` / `IslandBarGeometry.avatars` 决定：外接屏最多 `IslandMetrics.maxAvatars` 8 个，有真实刘海时收紧到 `notchAvatars` 4 个，放不下时最后一格换成 `IslandOverflowBadge` 的「+N」徽标——徽标颜色取被藏起来的会话里最需要关注的状态（等你回答橙 / 运行中蓝 / 其余灰），完整列表在展开态里看），含运行中 / 等你回答，以及已结束与已终止但还没在终端看过的（`acked != true`），排序同列表；`IslandAvatar` 是圆角方块脸 + 下方状态灯，底色由会话 ID 的稳定哈希从 8 色板取值，状态灯用会话状态色：蓝=运行中 / 橙=等你回答 / 绿=已结束 / 红=已终止，运行中 / 等你回答时缓慢呼吸），没有活跃会话时在最左侧用置灰的 bobo（默认图标）占位。头像资源 `bobo-island-avatar-*.png` **已把生成时烤进图里的深色背景扣成透明**（否则纯黑的方形卡片会和刘海背景糊在一起，只剩脸浮着）：素材只留 bobo 与它抱着的彩色牌子；牌子上再叠一枚来源牌面（`badgeColor` = `IslandMetrics.providerBadgeColor`，白 logo 用模板图染色），OpenCode 的**品牌黑也提亮成了深灰**（`Color(red:0.30,green:0.29,blue:0.29)`），同样是为了不在纯黑底上消失，换素材时别把这两处改回深色背景 / 纯黑。折叠胶囊里的图标规格全部走 `IslandMetrics`（外框 22、间距 5、悬停底色 0.12→0.22；头像脸 19 且外框也按 19 算，圆环外沿正好 22，画面里每段间距看起来才一致；设置 / 退出这类 SF Symbol 图形与额度圆环里的来源图标同尺寸 `quotaIconSize` 13）：bobo 头像、额度圆环（`IslandQuotaChip`，环外圈就是胶囊端帽，只显示圆环、不显示百分比——数字在悬停明细卡里，宽度恒为一枚图标；圆环画的是这一家自己的显示范围（`IslandQuotaProvider.displayWindow`：服务端记住的 `range`，认不出就退回 5 小时窗口），默认的「展开并排」方式在面板展开时按 `IslandBarGeometry.quotaChips` 算出的数量并排显示各来源、当前选中的那枚带底色，点某一枚圆环循环这一家的显示范围（5 小时 / 本周 / 账单月…）并把它设为折叠胶囊那家；「点击切换」方式保持原来的单圆环点击循环来源）、设备指示（`IslandDeviceChip`，额度右边：外环上 260° = 磁盘占用（白）、下 100° = 内存占用（青），两段在左侧（约 8 点）与右侧（约 4 点）各留 14° 缺口；进度都从**左侧**那端起长——存储顺时针往上绕过顶部，内存逆时针沿下弧向右（下弧顺时针会顶进存储那一段），`arc(clock:sweep:)` 的 `clock` 以 12 点为 0 顺时针，配色 ≥85% 红 / ≥75% 橙 / 其余健康色，中心圆点 = CPU 压力灯，`device` 字段缺失时整个不出现）、头像区右侧的分隔竖线（1×14 的 `Capsule`，两边间距同样是 `itemSpacing`）与设置 / 退出按钮共用同一套外框和间距。**设置齿轮与退出电源只在鼠标移上来（`hovering`）时出现**，不悬停时不显示也不占宽度（`islandSize` 里按 `hovering` 决定是否加上这两个 22）；`NotchIconButton`（参考 CodeIsland 同名按钮）做的齿轮点击与点胶囊一样打开主窗口，它右边的电源按钮（`power`，红色）走和菜单「退出 bobo」同一条 `NSApp.terminate` 路径（`applicationWillTerminate` 顺带停掉本地服务）。展开态才列出会话（每行右下角是**计时标签** + 来源图标 + 状态文案：`IslandSessionTag` 显示会话开始至今的时长，包在 `TimelineView(.periodic(from: .now, by: 1))` 里，展开着面板时每秒只重画这一格文字——`IslandMetrics.elapsedText` 的分档是 `<1m` / `5m` / `2h` / `1d`，与 CodeIsland 的 SessionTag 一致，没记到开始时间的老数据不显示；再左边是那一行的来源图标，`IslandRow.agentIcon` 按 `IslandMetrics.providerId(forSource:)` 取 `bobo-provider-<id>.svg`，六个来源各有一份真实品牌图标，加载不到才退回 SF Symbol）。
- 额度 / 设备指示的**悬停明细卡**：鼠标停在指示上满半秒弹出（`detailHoverChanged`，`IslandMetrics.detailDelay`），额度卡显示鼠标正停的那一枚圆环对应的来源（`IslandModel.quotaFocus`，并排显示时跟着换，鼠标移到卡片上内容也不跳；没有焦点时退回 `quota.displayed` 当前这一家）。卡片里「展开并排」模式的提示是「点击圆环切换显示范围」（这一家有不止一档窗口时），「点击切换」模式仍是「点击圆环切换来源」；并排模式下折叠胶囊那家带一枚「当前」徽标（`IslandBadge`，与本机估算共用同一套观感），圆环当前画的那一档窗口用加粗提亮的标题标出（不用徽标：agy 的标签很长，多出来的宽度会把标题挤到换行）。内容由 SwiftUI 的 `IslandDetailView` 渲染在独立的无边框小窗（`detailWindow`，层级比面板再高一级——点击面板会把它提到同层最前，卡片必须始终压在上面；同套深色圆角观感），尺寸由卡片自己量好后上报（`IslandModel.detailMetrics`，`kind` 是逻辑显示状态），位置按弹出时的鼠标位置锚定（`detailAnchor`：水平居中、顶边在鼠标下方 14pt，clamp 进屏幕）——数据刷新导致行数变化只重排、不跟着鼠标跑。保留 / 收起：鼠标离开指示后 0.4 秒内落在卡片上就留着（卡片自己的 `mouseExited` 会再查一轮，`scheduleDetailHide`），离开面板 / 卡片 0.35 秒后一起收起（`islandHovering` 到点检查鼠标在面板还是卡片上）；换屏、拖动面板、隐藏面板（`setNotchVisible(false)`）、打开主窗口都立刻 `hideDetail`。卡片的颜色阈值与圆环 / 弧共用 `IslandMetrics.remainingColor` / `usageColor` / `levelColor`，重置时间的说法与网页「用量」一致（`resetText`）。卡片弹出时（`showDetail`）会按 `kind` 调 `refreshDetail` 让服务端立刻采一次最新数据（额度 → `GET /api/usage?refresh=1`，设备 → `GET /api/devices?refresh=1`），数据变了再经状态流推回来。
- 应用窗口无标题栏：页面自绘顶栏，红黄绿灯所在的顶部 28pt 由 `TitlebarDragView`（原生拖拽层）覆盖，页面顶栏需留 `padding-top` 让交互元素避开这 28pt。应用加载 `/?app=1`，`app.js` 据此加 `body.app`：`body.app .brand` 绝对定位到顶部标题栏（和红黄绿灯同一水平线），顶栏 grid 里 `.top-links` 必须显式 `grid-column:3`、`.view-tabs` 显式 `grid-column:1`，否则中间列塌陷后右侧的 GitHub / 版本入口会被自动排到中间。`public/style.css` 是一套深色优先的卡片式界面语言（深底、卡片、蓝色强调、宽松间距；参考现代 Agent 管理器的观感），默认用 `prefers-color-scheme` 适配深色，另有两套 `:root[data-theme=light|dark]` 覆盖块供「设置 → 外观」手动固定（`app.js` 读写 `localStorage` 的 `bobo-theme`，并兼容读取旧的 `skills-theme`）；**新增颜色请同时改这三处，三种外观都要保持可读**。几个约定：`--shadow` 为 `none`（靠描边分层，不用阴影）；智能体视图中间列的条目是卡片（`.agent-items .skill-item`），侧栏条目是普通行；`#search` / `#agentSearch` 的放大镜图标是 CSS `data:` URI，靠 CSP 的 `img-src 'self' data:` 放行，改图标只需改这两条规则。
- 窗口显隐动态切换激活策略：应用以 `.accessory` + `LSUIElement` 启动（无 Dock 图标），`showWindow()` 切到 `.regular` 出现在 Dock 与 ⌘Tab，关窗走 `hideWindow()` 切回 `.accessory`。不要改成固定 `.regular`，否则关窗后会一直占用 Dock。

## 约定与陷阱

- 文案、错误消息、注释均为中文，保持一致。
- `normalize()` 是 `/api/command` 的参数白名单：只允许 `list`/`add`/`remove`/`update`/`init`，拒绝 `-p`/`--project`，`add` 会先做一次来源解析（不合法直接 400，不用等任务跑起来）。界面按钮传的 `-g`/`-y` 只是保留习惯写法，本机实现本身就是全局、免确认。
- `safeFile()` 拒绝绝对路径、隐藏文件/目录、`..` 和符号链接；文件保存校验版本 hash，冲突返回 409。放宽会破坏测试与安全模型。
- 备份规则：编辑文件、`add`/`update` 前、智能体写操作前备份到 `~/.bobo/backups/<时间>-<操作>/`，只保留最近 3 份（LRU 淘汰最旧，技能与智能体共用配额，服务启动时也清理一次）；`remove` 不备份，直接删除技能目录、各 Agent 链接与锁文件记录。调整保留数量时同步更新 `pruneBackups` 的调用与测试。
- 「我的 Skill」的技能在 `~/.agents/skills` 里是指向用户目录的符号链接，`sources.mjs` 管理这批链接；`skills.mjs` 只认 canonical 里的真身，遇到这种链接（指向 `~/.agents/skills` 之外）会拒绝删除，所以删除必须走 `sources.removeSkill`。`unlinkUnder` 只解引用确属该目录的链接，放宽会误删用户文件。
- 「我的 Skill」的停用名单保存在 `sources.json` 各目录的 `disabled` 数组：`relink`/`syncAll` 跳过停用项，停用只删 `~/.agents/skills` 链接、不删源文件；启用前校验同名冲突，删除技能和新建同名技能时要清理名单。
- 「智能体」把 `~/.config/opencode/agents` 里的文件名（含子目录）当作 ID：重命名就是换 ID，已有会话的 `@旧名` 引用会失效；V2 不读取 frontmatter 的 `name` 字段，只作为信息展示。停用只在 frontmatter 写 `disabled: true`，不移动文件。不要程序化改写 `opencode.json(c)` 的 `agents` 字段（JSONC 注释会丢），也不要管理内置 agent（build/plan/general/explore 没有文件）。
- 「智能体」的 Codex 来源把 `~/.codex/agents` 里的 `.toml` 文件当作 ID，但 **Codex 实际以 TOML 的 `name` 字段识别智能体**：重命名只移动文件名，不改 `name`（界面只有 OpenCode 支持「停用」，Codex 没有这个概念）；列表会提示文件名与 `name` 不一致。写操作（save/create/rename/delete）走 `/api/codex/agents/*` 并调用 `codexReader.invalidate`，AI 缓存以 `codex-agent:` 前缀与 OpenCode 智能体隔开。
- 「智能体」视图分两层：左侧栏只剩**来源**（`#agentSourceNav` 是一个空 `settings-menu`，按钮由 `renderAgentSources()` 按 `/api/configs/providers` 动态生成：图标一律是真实品牌图标（内联 SVG 精灵，没有就不放，不做首字母色块占位），行尾 `.notch-dot` 按 `installed` 置绿 / 灰，已安装的排前面；`renderAgentSources` 只更新差异、复用按钮节点并保持 `nav.scrollTop`，**不要改回「整列删了重建」**，否则点一下来源列表就会跳；底栏说明用 `footer>span` 的单行省略号保持高度恒定，来源名 / 路径长短不一时左栏可视高度不会跟着变）；右侧内容区 `main.agent-main` 改成 `flex-direction:row`，左边一列 `.agent-items` 是一个搜索框 + 区块（`renderAgentList`：**全局配置在前、智能体定义在后**，`agentSection` 渲染标题与数量，搜索词 `agentFilter` 同时过滤两块；全局配置由 `configs.mjs` 的 `GLOBAL_CONFIGS` 决定，未创建的文件也列出来，点进去编辑并保存即创建；`agentHasDefs()` 为假时不显示「智能体定义」区块与「＋ 新建」），右边是 `#agentEmpty` / `#agentDetail` 详情；`#agentDetail` 的 `.actions` 允许换行，窄窗口下操作按钮不会被裁掉。`agentDetailKind`（`'agent'` / `'config'`）记录当前详情类型，`agentMeta()` / `agentBackend()` 识别来源能力，`applyAgentDetailChrome` 据此隐藏配置的重命名 / 停用 / 删除 / 复制 ID。`app.js` 用 `agentIsConfig()` 分支：列表行由 `agentRow` / `agentConfigRow` 生成，详情复用智能体的阅读 / 编辑 / 能力总结 / 翻译（`openConfig`），Finder 打开配置目录（`POST /api/open {config:provider}`）。接口是 `GET /api/configs/providers`、`GET /api/configs?provider=`、`GET /api/configs/file?provider=&key=`、`POST /api/configs/save`，AI 走 `GET /api/ai/config/document?id=provider:key` 与 `POST /api/ai/config/generate`（`configReader`，缓存前缀 `config:`）。`⌘S` 与未保存提示复用智能体那套，`switchView('agents')` 先 `loadAgentProviders()` 再 `loadAgentAll()` 并发加载两块内容。
- 通知岛把 OpenCode、Codex、omp、Claude Code、DeepSeek Harness 与 Antigravity 的会话合并成一份 `sessions`（各自带 `source`、`order` 与 `startedAt`），刘海面板与网页按 `source` 区分、点击分别路由到 `opencode.acknowledge` / `codex.acknowledge` / `omp.acknowledge` / `claude.acknowledge` / `dsh.acknowledge` / `agy.acknowledge`（**点击就一定标记已查看**，终端页关掉、跳不过去也算，已结束 / 已终止的会话随即从列表收起）。`startedAt` 是**会话开始时间**（epoch 毫秒，只记一次、活动事件不重置），各来源分别取值：OpenCode 的 `time.created`（列表 / `session.created` 的 `info.time.created`，认不出来退回首次见到它的时刻）、Codex 的 `session_meta.payload.timestamp`（退回外层 timestamp 与文件时间）、omp 的 header `timestamp`（退回文件名前缀里的时间）、Claude Code 的活跃注册表 `startedAt`（退回会话日志首行 timestamp）、dsh 的 `identity.createdAt`（退回文件时间）、agy 的 `last_user_input_time`（退回 `last_modified_time`）；刘海面板与网页的会话行用它显示计时，新增来源时也要给出这个字段。新增会话来源时沿用这套：给出 `order`、`source` 与 `startedAt`，并接入 `terminals.viewed` 的「已查看」轮询与「通知岛」状态流的订阅（少一个 `subscribe` 就只在别的来源变化时才跟着刷新）。
- 智能体的能力总结/翻译走 `/api/ai/agent/*` 与 `agentReader`；智能体的写操作（save/rename/delete/toggle）必须调用 `agentReader.invalidate`，否则改完文件仍会命中旧摘要/译文。全局配置同样走 AI（`/api/ai/config/*` 与 `configReader`，`reader-ai.mjs` 里 `noun:'配置'`、`subject:'config'` 决定提示词文案），id 是 `provider:key`、缓存前缀 `config:`，保存后要 `configReader.invalidate`。技能、智能体与配置的 AI 缓存以 id 前缀区分，不要把前缀去掉，否则同名 id 会互相覆盖。
- 代码风格高度紧凑（单行函数、少空格），没有格式化工具；改动跟随周围写法，不要整体重排。
